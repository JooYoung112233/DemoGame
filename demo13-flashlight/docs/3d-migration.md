# 3D 쿼터뷰 전환 (2D → 3D)

> 2026-09-07 결정. **URP 2D Renderer / XY 평면 / Rigidbody2D**를 들어내고
> **URP 3D Renderer / XZ 평면 / Rigidbody + 실제 조명·그림자**로 재구축한다.
> 게임 로직(인벤·아이템·제작·퀘스트·NPC·스토리·경제·세이브·UI)은 **유지**한다.
>
> - 현 구현 상태의 진실은 여전히 [`rendering.md`](rendering.md) — 이 문서는 **계획**이다.
> - 전환이 끝나면 rendering.md를 3D 기준으로 덮어쓰고 이 문서는 이력으로 남긴다.
> - 이전 방향 전환(아이소 3D → 탑다운 2D, 2026-06-02) 기록은 [`topdown-migration.md`](topdown-migration.md).

## 결정

| 날짜 | 질문 | 결정 |
|------|------|------|
| 2026-09-07 | 전환 강도 — 물리·좌표계까지 바꾸나? | **완전 3D.** XZ 평면 + Rigidbody/3D 콜라이더 + URP 3D Renderer + 실제 조명·그림자. 하이브리드(3D 모델 + 2D 물리) 유지안 폐기 |
| 2026-09-07 | 카메라 | **고정 쿼터뷰 · 오소그래픽.** 각도 고정, 직교 투영 → 화면 어디서나 거리감 동일(루팅·전투 판정 공평). 퍼스펙티브·자유회전 폐기 |
| 2026-09-07 | 높이(Y축) 도입 범위 | **단차·엄폐까지.** 바닥은 평면 유지, 난간·차량·낮은 벽 뒤 엄폐를 높이로 표현. 2층·계단·옥상 풀세트는 **이번 범위 밖**(후속 판단) |
| 2026-09-07 | 캐릭터 외 아트 범위 | **전부 3D 재제작.** 적·NPC·프랍 63종/80프리팹까지 Blender 스크립트로 생성. 빌보드 스프라이트 혼용 폐기. 단 **아이템 아이콘 204종은 UI라 그대로 유지** |

## 왜 이번엔 되는가

2026-06-02에 아이소메트릭(2.5D 하이브리드)을 버리고 2D로 후퇴한 이력이 있다. 당시 사유와 지금 상황:

| 당시 폐기 사유 | 2026-09-07 현재 |
|---|---|
| AI로 아이소 스프라이트를 뽑으면 **투영각이 매번 달라 안 맞물림** | **해소.** 아트를 Blender 스크립트로 직접 생성한다(`build_reclaimer.py`). 투영각이라는 개념 자체가 없어짐 — 모델 하나를 어느 각도로든 렌더 |
| 깊이 정렬·접지가 계속 깨짐(2D-in-3D 이질감) | **해소.** 전부 진짜 3D 메시가 되면 Z버퍼가 정렬을 맡는다. `sortingOrder` 수동 관리 소멸 |
| 2인 제작에 비현실적 | **완화.** 로우폴리 단색 팔레트(텍스처 없음) + 절차 생성 스크립트 파이프라인이 이미 검증됨 |

반대로 **새로 생기는 비용**은 아래 "리스크"에 정리한다. 특히 카메라 오클루전(건물이 플레이어를 가림)은 2D에 없던 신규 문제다.

## 실측 — 무엇이 걸려 있나

2026-09-07 기준 `Assets/Scripts` + `Assets/Editor`:

| 항목 | 수치 | 의미 |
|---|---|---|
| 전체 C# | **66,933 LOC** / 207+45 파일 | |
| 2D API를 건드리는 코드 | **21,565 LOC (32%)** | 나머지 68%는 뷰 무관 로직 — 손 안 댐 |
| `Rigidbody2D` | **9 파일** | 물리 전환의 실제 표면은 좁다 |
| `Physics2D.*` 호출 | **10곳** | OverlapBox 2 · SyncTransforms 2 · Raycast/RaycastNonAlloc/OverlapPointAll/OverlapCircle/Linecast/CircleCast 각 1 |
| `Collider2D` | 19 파일 | |
| `Light2D` | 11 파일 | 조명 재작성 대상 |
| `ShadowCaster2D` | 3 파일 | 3D에선 메시가 알아서 그림자를 던짐 → 삭제 대상 |
| `SpriteRenderer` | 33 파일 | **대부분 HP바·라벨·아이콘** — 빌보드로 살거나 그대로 감 |
| `sortingOrder`/`sortingLayer` | 64 파일 | 월드 깊이용은 삭제, UI/오버레이용은 유지 — **선별 필요** |
| 씬 | 21개 | 대부분 **코드 생성물**(아래) |
| Prop2D 카탈로그 | SO 63 / 프리팹 80 | 3D 재제작 대상 |

### 🟢 결정적으로 유리한 세 가지

1. **맵이 코드 생성물이다.** `Zone1.unity`(69,587줄)는 손으로 배치한 게 아니라 `Zone1GreyboxLayout.cs`(2,223 LOC)가 찍어낸다. 좌표가 이미 **1u = 1m 실측 미터** 기준이라 `new Vector3(x, y, 0f)` → `new Vector3(x, 0f, y)` 매핑이 전환의 큰 축이다. 씬 21개를 손으로 옮기는 작업이 아니다.
   - 빌더: `Zone1GreyboxLayout`(2223) · `ScrapMarketGreyboxLayout`(500) · `SafehouseGreyboxLayout`(309) · `InteriorBuild`(246) · `GreyboxBuild`(274) · `HideoutGreyboxLayout`(201) · `PawnshopGreyboxLayout`(200) · `GameSceneBuilder`(203)
2. **세이브에 월드 좌표가 없다.** `GameSaveData`는 아이템·퀘스트·플래그·통화·평판 상태만 담는다. **세이브 마이그레이션 불필요, 기존 세이브 호환.**
3. **URP 3D 에셋이 안 지워지고 남아 있다.** `Assets/Settings/URP-3D.asset` + `ForwardRenderer.asset`. 파이프라인 전환은 에셋 제작이 아니라 **설정 교체**다.

## 유지 / 교체 / 신규

| 🟢 유지 (뷰 무관) | 🔄 교체 (2D → 3D) | 🆕 신규 |
|---|---|---|
| 인벤토리·컨테이너·아이템·무기파츠 | `Rigidbody2D`+`Collider2D` → `Rigidbody`+3D 콜라이더 | **카메라 오클루전 페이드**(건물이 플레이어를 가릴 때) |
| 제작·의료·생존·수면 | `Physics2D.*` 10곳 → `Physics.*` | **단차·엄폐 데이터**(높이 있는 콜라이더 + 엄폐 판정) |
| 퀘스트·NPC·대화·스토리·평판 | `Light2D` → `Light`(Directional 낮밤 + Point/Spot 랜턴) | **3D 프랍 카탈로그**(`Prop3DDefinition`) |
| 경제·상점·레이드·탈출·정산 | `ShadowCaster2D` → 메시 실제 그림자(삭제) | **프랍 Blender 생성 스크립트**(63종) |
| 세이브·체크포인트 (좌표 없음) | `GroundShadow2D`(202) → 실제 그림자 | **적·NPC 3D 모델 + 공격/피격/사망 애니** |
| **UI 전체**(uGUI 스크린스페이스 26패널) | 맵 빌더 좌표 `(x,y,0)` → `(x,0,y)` | 3D용 오소 쿼터뷰 카메라 리그 |
| 아이템 아이콘 204종 | `Prop2DDefinition`/`Prop2DBuilder`(615) → 3D판 | |
| StatDB·GameTuning·밸런스 | `PlayerVision` LOS: `Linecast` → `Physics.Linecast` | |
| 특성·퀵슬롯·도감 | ~~Spine 플레이어/적 → 3D 모델~~ ✅ Spine 제거 완료(2026-09-07) | |
| 나침반·미니맵·지도조각 (XY→XZ만) | 월드 라벨·HP바·말풍선 → 빌보드 | |

## 좌표계 규약 (전환 후 SSOT)

전환이 끝나면 이 표가 `rendering.md`의 "한눈에" 표를 대체한다.

| 항목 | 결정 |
|---|---|
| **월드 평면** | **XZ** (바닥). up = **+Y**. 축척 **1u = 1m** (기존 유지 — 맵 빌더 좌표 그대로 재사용) |
| **좌표 변환** | 기존 2D `(x, y)` → 3D `(x, 0, y)`. 즉 **구 Y가 신 Z** |
| **깊이** | **Z버퍼(실제 3D)**. `sortingOrder` 수동 정렬 폐기 — UI/오버레이 전용으로 격하 |
| **카메라** | **오소그래픽 고정 — pitch 55° / yaw 0° / orthographicSize 7**(세로 14m). Stage 0 확정. ⚠️ 카메라-대상 거리는 **그림자 거리(50m) 안**이어야 한다(아래 함정) |
| **물리** | `Rigidbody`(capsule) + `freezeRotation`. 중력 켬(단차 낙하). NavMesh는 후속 선택 |
| **조명** | Directional(낮밤) + Point/Spot(랜턴·프롭 발광) + 실제 그림자 |
| **가시성** | 시야 FOV 유지 — LOS만 `Physics.Linecast`로. 어둠 오버레이는 3D 포그/후처리로 재작성 |
| **아트** | 로우폴리 단색 팔레트, 텍스처 없음. Blender 스크립트 생성 |

## 2D 정리 장부 (무엇을 언제 지울 수 있는가)

> 2026-09-08 실측. **"2D 흔적을 지금 다 지우면 안 되나"에 대한 답이 여기 있다.**
> 결론: 2D는 아직 잔재가 아니라 **레이드 전체가 올라가 있는 살아 있는 층**이다.
> 3D로 넘어간 곳은 **은신처와 마을 두 곳뿐**이고, 전투·맵·조명은 전부 2D가 정본이다.
> 지우는 순서는 항상 **포팅 → 참조 교체 → 삭제**다. 순서를 건너뛰면 컴파일이 깨진다.

| 대상 | 실측 | 무엇이 막고 있나 | 풀리는 시점 |
|---|---|---|---|
| ~~전투 12파일~~ | ✅ **2026-09-08 이식 완료** — 2D 물리 참조 0 | — | 완료 |
| `Prop2DDefinition` · `Prop2DBuilder` | 씬·프리팹 **63곳**이 `Prop2DDefinition`을 참조 | 레이드 맵 소품이 전부 이 카탈로그로 서 있다 | **Stage 3** (`Prop3D*` 대체 후) |
| `BuildingEntrance` | 씬 **18곳** 참조, 코드 14곳 | 2D 실내 진입이 아직 이걸로 돈다. 3D는 `SceneDoor3D` | Stage 3 (실내씬 3D화) |
| `Light2D` 11파일 (`DayNightCycle` `PropLight2D` `FlashlightController` `VisionDarkness` …) | 파일당 1~8회 | 낮밤·시야·랜턴이 전부 2D 조명 위에 있다 | **Stage 2** |
| 레이드 맵 씬 | `Int_*` **15개** + `Zone1` + `ScrapMarket_GB` | 3D 대응 **없음**. 지우면 게임에 갈 곳이 없다 | Stage 3 |
| 2D 씬 이름 문자열 (`"Safehouse"` `"Hideout"` `"Pawnshop"`) | 코드 **13파일 25곳** — 스토리 트리거·QA·세이브 체크포인트·디버그 UI | 씬만 지워도 흐름이 끊긴다. **문자열 교체가 선행**되어야 한다 | Stage 3 이후 |
| `HideoutGreyboxLayout.cs` · `SafehouseGreyboxLayout.cs` | 3D 빌더가 1:1 대체 | 없음 — **지금도 지울 수 있다.** 다만 2D 씬을 다시 만들 수단이 사라진다 | 지금 (보류 중) |

### 왜 아직 못 지우는가

~~전투 12파일이 병목이다~~ → **2026-09-08 해소.** 전투 계열은 3D 물리로 옮겨 2D 참조 0.
남은 병목은 **맵 층**이다:

- `Prop2DDefinition`이 씬·프리팹 63곳을 붙들고 있어 레이드 맵이 통째로 2D다.
- `Light2D` 11파일 위에 낮밤·시야·랜턴이 서 있다.
- 2D 씬 이름이 코드 13파일 25곳에 문자열로 박혀 있다.

그래서 다음 전제 조건은 **Stage 2(조명)와 Stage 3(맵)**이다.

#### Stage 1에서 실제로 무엇이 바뀌었나 (2026-09-08)

컴파일이 안 되는 문제보다 **컴파일은 되는데 조용히 틀리는 문제**가 많았다. 기록해 둔다:

- `(Vector2)transform.position` — 3D에서 `(x, 높이)`로 읽힌다. Z가 통째로 사라져 거리·방향이
  전부 틀어지는데 오류는 안 난다. 전투·플레이어 범위 24곳 중 해당분을 `Plan3D.ToPlan`으로 교체.
- `BodyZones.FromPoint`가 `Vector2.y`를 **키**로 썼다 — 3D에선 키가 월드 Y다. 월드 `Vector3` +
  좌우 기준축을 받도록 변경.
- `PlayerVision.CanSee(Vector2)` — 호출부가 `Vector3`를 넘기면 암묵 변환으로 높이를 Z로 읽었다.
  시그니처를 월드 `Vector3`로.
- `SpawnZone.GetRandomPoint2D`가 **y(높이) 방향으로 흩뿌렸다** — 적이 공중·땅속에 생긴다.
  XZ판과 중복이라 하나로 합침.
- 물리 검사 높이: 히트박스·시야·길찾기 LOS를 **지면이 아니라 몸 높이**에서 쏜다. 지면에서
  쏘면 바닥·연석·문턱에 전부 걸려 "항상 막힘"이 된다.
- 사거리는 **평면 거리**로. 단차 위 적을 때릴 때 높이차가 사거리를 먹으면 안 된다.

### 지금 지키는 규칙

- 3D 신규 코드는 2D 파일을 **고치지 않고** 옆에 새로 만든다(`SceneDoor3D`가 `BuildingEntrance`를 안 건드린 것처럼).
- 2D 전용 파일은 **지우지 말고 그대로 둔다.** 반쯤 지우면 어느 쪽이 정본인지 알 수 없어진다.
- 3D가 대체를 끝낸 것만 이 표에서 지우고 실제 삭제로 옮긴다.

## 단계 계획

각 단계는 **끝에 돌려볼 수 있는 상태**로 닫는다. 전체 작업은 전용 브랜치에서 한다 — 파이프라인 전환이 전역이라 중간 상태로 main에 섞이면 안 된다.

### Stage 0 — 스파이크 ✅ **완료 (2026-09-07)**

`Assets/Scenes/Sandbox3D.unity` + `Assets/Shaders/SpikeOccluder.shader`(스파이크 전용).
기존 2D 설정을 안 건드리려고 **URP-2D 파이프라인에 ForwardRenderer를 인덱스 1로 덧붙이고**
이 씬 카메라만 그것을 쓴다 — 되돌리기는 렌더러 목록 한 줄 제거.

**확정된 값**

| 항목 | 값 | 근거 |
|---|---|---|
| 카메라 각도 | **pitch 55° / yaw 0°** | 사용자 판단. yaw 45°(대각선)는 입체감이 가장 좋았지만 채택하지 않았다 — 맵 축이 화면축과 어긋난다. pitch 55°가 60°보다 상자 두께·벽 옆면이 보여 입체감이 낫다 |
| orthographicSize | **7** (세로 14m) | 캐릭터 가독성 우선. ⚠️ 시야가 좁아 적을 늦게 볼 여지 — 레이드 검증 시 재확인 |
| 캐릭터 스케일 | **1.0** = 실측 2.08m | 1u=1m 세계 기준 |
| 오클루전 | **화면 공간 컷어웨이**(원형 구멍 + 디더 소프트 엣지) | 아래 |

**오클루전 4종 비교 결과** — 무처리는 캐릭터가 완전히 사라져 논외.
*디더 페이드*는 건물 하나가 통째로 유령이 되고 뒤가 다 비쳐 공간이 헷갈린다.
*자동 숨김*은 건물이 그림자까지 통째로 사라져 이동할 때마다 깜빡인다.
**컷어웨이**만 건물의 덩어리감을 유지하면서 플레이어 가시성을 보장한다 — 레이드 게임에서
"저기 건물이 있다"는 전투·동선 판단의 근거라 지우면 안 된다.

구현 부담은 작다: `Shader.SetGlobalVector`로 플레이어 화면 좌표를 전역 유니폼 하나로 뿌리면
모든 건물 머티리얼이 재질별 작업 없이 동작한다.

**pitch 55°에서 재검증(2026-09-07)** — 각도를 눕히면 오클루전이 줄 것이라는 예상은 **틀렸다.**
9m 건물이 앞에 서면 캐릭터가 그대로 완전히 사라지고, ortho 7로 당긴 탓에 그 건물이 화면의
절반 가까이를 먹어 오히려 더 심하다. 컷어웨이 결정은 이 각도에서 더 필요하다.

**✅ 스파이크 셰이더 완성형**(`Spike/OccluderFX`, Mode 1):
- 판정 = ① 화면상 플레이어 반경 안 **∩** ② 프래그먼트가 플레이어보다 카메라에 가까움.
  ②가 없으면 뒤에 있는 건물까지 뚫린다.
- 파라미터는 **전역 유니폼**(`_CutCenter`/`_CutRadius`/`_CutSoft`/`_CutEnabled`) —
  `Shader.SetGlobalVector` 한 번이면 이 셰이더를 쓰는 모든 건물이 동작한다(재질별 배선 없음).
  실제 게임에선 카메라 리그가 매 프레임 갱신한다.
- 반경은 픽셀이 아니라 **화면 높이 대비 비율**(0.13) — 해상도가 바뀌어도 안 틀어진다.
- 그림자는 구멍과 무관하게 그대로 드리운다(건물이 사라진 것처럼 보이면 안 된다).

**도중에 발견한 함정 3건**

1. **그림자 거리** — 오소 카메라를 60m 뒤에 두면 대상이 그림자 거리(50m) 밖이라 그림자가
   **전혀 안 나온다**. 오소는 카메라 거리가 화면에 영향을 주지 않으므로 30m로 당겨 해결.
   3D 카메라 리그에 이 제약을 박아둘 것.
2. **컷어웨이는 월드 거리로 하면 안 된다** — 건물 표면이 플레이어에서 9m 떨어져 있어 반경
   안에 들어오지 않는다. 가리는 것을 걷어내는 일이므로 판정은 **시선 축(화면 공간)** 에서.
3. **캐릭터 가독성** — 올리브/갈색 팔레트가 회색 바닥·갈색 상자와 붙어 눈에 먼저 안 들어온다.
   아웃라인 또는 접지 링이 필요하다. Stage 4 아트 작업에 포함할 것.
   **컷어웨이를 뚫어도 이 문제는 남는다** — 구멍 안쪽이 건물 내부라 그늘져서 어두운 캐릭터가
   더 묻힌다. 후보: 아웃라인 / 접지 링 / 구멍 안쪽 밝히기. **아웃라인 + 접지 링** 권장.

### Stage 1 — 좌표계 · 물리 전환
**목표:** 플레이어와 적이 XZ 평면에서 3D 물리로 움직이고 싸운다.
- `TopDownPlayer`(951) · `EnemyController`(1054) · `AttackPerformer`(215) · `ThrowSystem`(251) · `PlayerGun`(266) · `Breakable`(454) · `InteractableObject`(551) · `DoorController`(288) 물리/좌표 전환.
- `Physics2D.*` 10곳 → `Physics.*` (`OverlapBox`→`OverlapBox`, `CircleCast`→`SphereCast`, `Linecast`→`Linecast`, `OverlapPointAll`→카메라 레이캐스트).
- `FacingDirection`: 마우스 스크린 좌표 → **바닥 평면 레이캐스트**로 조준점 산출.
- `ChibiPlayerVisual`의 임시 기울기 보정 제거(진짜 3D가 되면 불필요).
- **대상 씬:** `CombatSandbox` 하나로 먼저. 지역/안전가옥은 손대지 않음.
- **검증:** 샌드박스에서 이동·구르기·근접·사격·투척·파괴가 전부 동작.

### Stage 2 — 렌더 파이프라인 · 조명 · 가시성
**목표:** URP 3D로 갈아타고 낮밤·시야·어둠이 3D에서 성립한다.
- `GraphicsSettings`/`QualitySettings` → `URP-3D.asset`. `TransparencySortMode` 커스텀축 해제.
- ~~`Light2D` → 3D 라이트~~ ✅ **런타임 이식 완료**(2026-09-08). `DayNightCycle`·`FlashlightController` 3D화. `VisionDarkness`·`DenseAnomalyController`·`TopDownPlayer`의 Light2D 언급은 **주석·툴팁뿐**이라 의존 없음. `DayNightLightDriver`(어디서도 안 씀)·`PropLight2D`(2D 프롭 전용, 3D는 `Prop3DBuilder`가 이미 점광을 붙인다)는 **죽은 코드** → Stage 5에서 2D 프롭 파이프라인과 함께 삭제. 남은 `Light2D` 참조 16파일은 전부 2D 전용 에디터 툴.
- ~~낮밤·조명 3D 재작성~~ ✅ **완료 + 실측 검증**(2026-09-08). 맵 조명 셋업은 `Lighting3D` 한 곳(프리셋 Outdoor/Indoor/Hideout). 낮밤 3중 버그 해소(WeatherData 미연결 / Trilight에서 `ambientLight` 무시 / 씬 전환 훅 부재) — 변경 로그 참조.
- `PlayerVision`(148) LOS 3D화, `VisionDarkness`(155)·`DenseAnomalyController` 어둠/현상 오버레이를 3D 포그·후처리로 재작성.
- **Stage 0에서 정한 오클루전 페이드 구현.**
- `sortingOrder` 64파일 **선별 감사** — 월드 깊이용은 제거, UI/오버레이용은 유지.
- **검증:** 샌드박스에서 낮↔밤 전환, 랜턴 착용, 시야 콘 밖 적 은폐, 벽 뒤 차폐가 전부 동작.

### Stage 3 — 맵 파이프라인
**목표:** 맵이 3D로 생성된다.
- `Prop3DDefinition` + `Prop3DBuilder`(구 615 LOC 대체): 메시·머티리얼·콜라이더(Box/Mesh/Convex) + **높이**.
- **프랍 63종 Blender 생성 스크립트** — 캐릭터와 같은 파이프라인(`tools/`).
- 맵 빌더 좌표 매핑 `(x,y,0)`→`(x,0,y)` + 벽/건물에 높이 부여 + 단차·엄폐 배치.
- **순서:** 안전가옥 → 은신처 → 실내씬(`InteriorBuild`) → 고철시장 → 지역1(가장 큼).
- **검증:** 각 맵 빌더 재실행 → 씬 생성 → 걸어다니며 충돌·엄폐·조명 확인.

### Stage 4 — 캐릭터 · 적 · 월드 오브젝트
**목표:** 화면에 2D 스프라이트가 남지 않는다.
- 적·NPC 3D 모델 + **공격·피격·사망 애니**(현재 치비는 idle/walk/run만 있음 — 전투 모션 부재가 지금 최대 구멍).
 - **상태→모션 매핑 재구현**: 구 Spine 구동부가 갖고 있던 매핑을 3D 애니메이터로 옮긴다 —
   구르기=`roll` / 약·강공격=`attack` / 앉기=`sit`·`sit_walk` / 이동=`walk`·`run` / 정지=`idle`.
   원샷 애니(roll/attack) 끝까지 1회 보장 로직도 함께(루프 애니가 중간에 자르지 않도록).
 - **`HitFlash`·`InjuryVFX` 재배선**: 둘 다 SpriteRenderer 바디를 가정한다. 3D 표시에선 그 SpriteRenderer가
   꺼져 있어 **플레이어 몸에 피격 연출이 안 보인다**(화면 효과는 정상). 메시 렌더러 기준으로 옮길 것.
 - ~~Spine 런타임·`cha` 에셋 제거~~ ✅ **2026-09-07 완료**(Unity 6.6 컴파일 불가로 앞당김).
- 월드 라벨·HP바·말풍선(`EnemySpeechBubble` 292)·퀘스트 마커(`NPCQuestMarker` 319)·`WorldItem`(184)·`InjuryVFX`(264) 빌보드화.
- **검증:** 지역1 레이드 완주 — 진입·전투·루팅·탈출·정산.

### Stage 5 — 정리
- 2D 잔재 제거(`Renderer2D.asset`/`URP-2D.asset`/2D 패키지/`Prop2D*`/`BRB` 2D 셰이더).
- `rendering.md`를 3D 기준으로 덮어쓰기, `topdown-art-spec.md` 폐기·대체, `char-art.md`/`prop-catalog.md`/`art-needs.md` 3D 기준 갱신, MASTER 색인 동기화.

## 리스크

| 리스크 | 대응 |
|---|---|
| **카메라 오클루전** — 2D에 없던 신규 문제. 건물·벽이 플레이어를 가린다 | Stage 0에서 방식을 먼저 정한다. 미정 상태로 Stage 3(맵)에 들어가면 맵을 두 번 만들게 됨 |
| **파이프라인 전환은 전역·비가역적** — 프로젝트 전체 머티리얼 룩이 한 번에 바뀜 | 전용 브랜치. Stage 1은 샌드박스 씬 하나로 한정. main 병합은 Stage 4 완주 후 |
| **전투 모션 부재** — 3D 캐릭터에 공격·피격·사망이 없다. 지금 상태로 전투에 넣으면 때려도 idle | Stage 4 전에 최소 attack/hit/death 3종 확보. Stage 1 검증은 모션 없이 판정만 확인 |
| **프랍 63종 재제작 물량** | 캐릭터 파이프라인이 이미 검증됨. 재질·형태를 파라미터화해 배치 생성. 그레이박스로 먼저 전부 채우고 순차 교체 |
| **적 AI가 평면 전제** — 현재 Rigidbody2D 스티어링, NavMesh 없음 | 단차만 도입하므로 평면 스티어링 유지 가능. 필요해지면 생성된 맵에 NavMesh 베이크(맵이 코드 생성이라 자동화 가능) |
| **중간 상태에서 게임이 안 돌아감** | 각 Stage를 "돌려볼 수 있는 상태"로 닫는다. Stage 경계에서 커밋 |

## 미결 항목

전환을 시작하기 전에 답이 필요한 것:

1. ~~카메라 yaw~~ ✅ **0° 확정**(2026-09-07 재결정). 한때 45°로 정했다가 사용자가 대각선을 원치 않아 0°로 되돌렸다 — 맵 축이 화면축과 그대로 맞는다. 입체감 손실은 pitch를 55°로 세워 일부 보전.
2. ~~오클루전 처리 방식~~ ✅ **화면 공간 컷어웨이 확정**(2026-09-07).
3. **2층·계단을 나중에 넣을 것인가** — 이번 범위는 단차·엄폐까지지만, 벽 높이·건물 구조를 지금 어떻게 만드느냐가 나중 2층 도입 비용을 좌우한다. 최소한 "벽은 실제 높이를 가진 메시" 정도는 지켜둘 것.
4. **브랜치·PR 전략** — 현재 `codex/chibi-survivor-3d`가 `origin/main` 기준 430 커밋 앞/1 뒤로 갈라져 있다. 3D 전환 브랜치를 어디서 딸지 정리 필요.

## 변경 로그

| 날짜 | 내용 |
|------|------|
| 2026-09-09 | **★ 적을 때려도 데미지가 0이었다 — 스폰된 적 44기 전부.** `EnemySpawner`가 조립 순서를 **허트박스 → Health**로 두고 있었다. `AddComponent`는 그 자리에서 `Awake()`를 부르므로, `Hurtbox.Awake`가 `GetComponentInParent<Health>()`를 부를 때 루트에 Health가 아직 없어 **null이 그대로 굳었다.** 그 결과 `ReceiveHitAt`이 아무 일도 하지 않았다 — 적은 플레이어를 정상적으로 때리는데(HP 100→41 확인) 플레이어의 타격만 먹지 않는, 한쪽만 도는 전투였다. 두 겹으로 고쳤다: ① `Hurtbox`의 `Health`·`Enemy`를 **지연 해석 프로퍼티**로(조립 순서에 안 휘둘린다) ② 스포너 순서를 Health 먼저로. **검증**: 타격 시 HP 33→21→9, 그로기 0→25→44%, 상태 `Hit`. **오늘 두 번째로 나온 같은 부류다** — 상호작용 하이라이트도 "빈 캐시가 굳어" 죽어 있었다. Awake에서 한 번 찾아 캐시하는 코드는 대상이 나중에 생기면 조용히 죽는다. |
| 2026-09-09 | **마을에 출전 지도판이 없어 레이드로 나갈 수단이 아예 없었다.** 3D 마을을 새로 지으면서 게시판을 장식용 `Prop`으로만 세웠고, `MapBoard` 상호작용(→ `MapSelectUI`)이 빠졌다. 핵심 루프의 "나가서"가 통째로 끊긴 상태. `Board_Dispatch`를 동쪽 게이트 길목(72, 24)에 추가. **검증**: 누르면 `MapSelectUI`가 실제로 열리고, 지역1(폐상가 교역 지구 → Zone1) 출전 시 `SP1_S`에 스폰, 적 44기·루팅 90개·탈출구(맨홀) 배치까지 확인. |
| 2026-09-09 | **게임을 시작하면 빈 2D 껍데기 마을로 들어가고 있었다.** 3D 마을·은신처가 `Safehouse3D`·`Hideout3D`라는 **별칭**에 있었고, 코드는 `"Safehouse"`를 26곳·`"Hideout"`을 7곳에서 참조한다. 그 이름의 2D 씬은 `Map` 오브젝트 하나뿐인 껍데기(스폰 0개)였다. 참조 33곳을 고치는 대신 **씬을 정식 이름으로 승격**했다(2D 껍데기 삭제 → 3D를 그 자리로 이동). 빌더의 `ScenePath`·내부 씬 이름 참조도 함께. **함정**: `Tools/TopDown/빌드/안전구역` 메뉴가 2D 빌더를 부르고 있어서, 승격 후 그 메뉴를 누르면 **마을이 통째로 덮여 사라진다** — 3D 빌더를 부르도록 바꾸고 2D 레이아웃 빌더 2종을 삭제했다. 빌드 세팅의 중복·유령 항목도 정리(23 → 21). |
| 2026-09-09 | **스폰 좌표가 틀어진 원인: 빌더가 `pointId`를 안 채웠다.** `Spawn()` 헬퍼가 **GameObject 이름만** 바꾸고 직렬화 필드 `pointId`는 기본값 `"default"`로 뒀다 — 그래서 마을 스폰 4개가 **전부 "default"를 자칭**했고, 어느 문으로 들어와도 가장 먼저 만들어진 스폰(전당포 앞)으로 떨어졌다. `Safehouse3DLayout`·`Greybox3D`(레이드 맵 스폰 9개)·`Hideout3DLayout` 모두 `pointId`를 이름과 함께 채우도록 수정하고 재생성. **검증**: 마을 스폰 4개 이름↔id 전부 일치, 실제 부팅→전환에서 플레이어가 `default`(53.5, 12)에 정확히 스폰. |
| 2026-09-09 | **`BRB/Stylized`가 추가 광원을 아예 안 받고 있었다.** 이 셰이더는 `GetMainLight`만 쓰고 `_ADDITIONAL_LIGHTS` 키워드도 루프도 없었다 — 즉 **점광·스포트가 전부 무시된다.** 실내 천장등도, 프롭 램프도, 새로 단 착용 랜턴도 화면에 한 톨도 안 나왔다. 스타일라이즈드를 맵·적·무기에 배선하면서 생긴 회귀다(전에는 URP/Lit이라 정상이었다). URP 기본 Lit과 **글자 그대로 같은** pragma(`multi_compile _ _ADDITIONAL_LIGHTS_VERTEX _ADDITIONAL_LIGHTS`)를 넣고, 주 광원과 같은 wrapped diffuse로 더하는 루프를 추가. **찾는 데 오래 걸린 이유**: 값·설정·거리·`isVisible`이 전부 정상이라 로그로는 안 보였고, 램프를 켠 화면과 끈 화면이 **픽셀 단위로 동일**하게 나와서야 드러났다. |
| 2026-09-09 | **⚠️ URP `LightRenderingMode` 열거형은 숫자 순서가 아니다 — `Disabled=0, PerPixel=1, PerVertex=2`.** 추가 광원이 안 나오길래 `m_AdditionalLightsRenderingMode`가 1이라 "PerVertex겠거니" 하고 2로 올렸는데, **원래 1이 이미 PerPixel이었고 2가 PerVertex라 오히려 떨어뜨렸다.** 되돌렸다(1). 에셋 YAML의 이 필드를 손댈 때는 반드시 열거형 정의(`UniversalRenderPipelineAsset.cs`)를 먼저 확인할 것. 오브젝트당 상한은 4 → **8**로 올려 둔다(천장등이 격자로 깔려 한 면에 여러 개가 닿는다). |
| 2026-09-09 | **⚠️ 검증 방법 자체가 틀렸던 건: `Camera.Render()` 수동 호출.** 스크린샷을 뜨려고 카메라를 RenderTexture에 직접 렌더했는데, URP에서 이 경로는 **추가 광원 세팅을 건너뛴다** — 그래서 램프가 안 보이는 게 실제 문제인지 촬영 방식 탓인지 한동안 구분이 안 됐다. 지금은 `ScreenCapture.CaptureScreenshot`으로 **실제 렌더 경로**를 그대로 찍는다(UI·후처리까지 화면과 동일). 3D 화면을 눈으로 확인할 일이 있으면 이쪽을 쓸 것. |
| 2026-09-09 | **직접 플레이해 보고 잡은 것 2건.** 스크린샷으로 확인. ① **시야 어둠이 사실상 안 보였다** — 알파가 밝은 경계(투명)에서 바깥 반경 60m까지 **선형으로 퍼져** 있어, 게임 줌(화면 높이 18m)에서 보이는 구간은 알파가 0.1밖에 안 됐다(설계값 0.72). 각도마다 정점을 2개 → **3개**(경계 → 경계+1.6m → 바깥)로 늘려 좁은 띠 안에서 어둠이 차오르게 했다. 이제 콘이 또렷하게 읽힌다. ② **밤이 거의 안 보였다** — 2D 시절 밤 조명은 `Light2D` 글로벌(강도 0.22 × 색 0.45,0.50,0.68)이 **모든 면을 균일하게** 밝히는 것이었는데, 3D 이식 때 그걸 디렉셔널 0.18로만 옮겼다. 균일 성분은 앰비언트가 맡아야 한다 → `nightAmbientColor` (0.04,0.04,0.06) → **(0.10,0.11,0.15)**(옛 글로벌 라이트 실효값). 밤은 여전히 어둡지만 콘 안이 읽힌다. |
| 2026-09-09 | **미결 — 밤에 플레이어가 검은 덩어리다.** 조종하는 캐릭터가 안 보인다. `PlayerRig`에 `PlayerLamp3D`(점광, y=1.25, range 5.5, 따뜻한 색)가 있는데 **오브젝트가 꺼져 있고 코드에서 아무도 안 건드린다.** 켜면 해결되지만 밤 룩이 "플레이어 주변만 밝은 거품"으로 바뀌므로 기획 판단이 필요하다 — 사용자 확인 대기. 관련: 손전등(`FlashlightController`)은 3D 스포트로 이식만 해 두고 어디에도 안 붙어 있다. |
| 2026-09-09 | **Stage 0에서 정한 3D 룩 셰이더가 어디에도 안 걸려 있었다.** `BRB/Stylized`(무광·부드러운 명암 wrap·림라이트·정점 AO)는 Stage 0에서 룩을 확정하며 만들어 놓고 **참조 0건**이었다 — 맵도 적도 무기도 전부 기본 `URP/Lit`이라 정해 둔 아트 방향이 화면에 전혀 반영되지 않았다. 특히 림라이트는 Stage 0 함정 ③(어두운 팔레트에서 실루엣이 배경에 묻힌다)에 직접 듣는 항목이라 손해가 컸다. 배선 위치 셋: `Greybox3D.Mat`(바닥·바리케이드·프롭), `Safehouse3DLayout.Lit`(지면·실내 바닥·셔터·문틀), `GreyboxMesh.Material`(적 몸·무기·떨어진 아이템). **벽·지붕·차량은 그대로 `Spike/OccluderFX`** — 컷어웨이가 우선이다(셰이더 주석의 역할 분담 그대로). 셰이더가 없으면 URP/Lit으로 조용히 폴백한다. |
| 2026-09-09 | **Stage 5 — 지울 것과 남길 것을 갈랐다.** 삭제(참조 0·씬 사용 0): `CameraSortSetup`(2D 정렬축 강제, URP-3D에선 Default), `DayNightLightDriver`(`DayNightCycle`이 완전 대체), `AnomalyHazePrototype`(+짝 셰이더, 어느 머티리얼도 참조 안 함). **보존 결정**: `Resources/Props2D/*.asset` **63개는 아직 만들어야 할 3D 프랍 63종의 사양서**다(`propId`·`displayName`·`category`·`group`) — 지우면 Stage 3의 스펙이 사라진다. 프리팹 80개와 `Prop2D*` 파이프라인도 그 스펙과 짝이라 3D 프랍이 나온 뒤에 함께 정리한다. 미사용 2D 셰이더 5종(`SpriteSheet`·`SpriteBillboard`·`PropPixel`·`FloorPixel`·`DecalPixel`)도 같은 시점에. |
| 2026-09-09 | **시야 어둠 오버레이가 원점에 선 검은 벽이었다.** `VisionDarkness`의 부채꼴 정점이 `(cos, sin, 0)` — **XY 평면**이라 3D에선 바닥을 전혀 안 덮고 수직으로 서 있었다. 위치도 `(c.x, c.y, 0)`이라 월드 원점 평면에 붙어 있었다. XZ 평면으로 펴고 플레이어 발밑 0.05m에 깐다. 셰이더도 `Sprites/Default`(빌트인 → URP에서 분홍 에러)였고, URP 기본 Unlit은 정점 색을 안 읽어 밝음↔어둠 그라디언트가 사라진다 → 전용 셰이더 **`BRB/VisionDarkness`** 신설(언릿·반투명·정점색·ZTest Always). `sortingOrder` 배선은 제거(3D에선 무의미, 순서는 Queue/ZTest가 정한다). **한계**: 이 오버레이는 **바닥만 덮는다** — 벽·서 있는 프롭은 안 어두워진다. 화면 전체를 덮으려면 후처리가 필요하고, 지금은 바닥 어둠 + `PlayerVision`의 적 은폐 조합으로 읽힌다. **검증(플레이)**: 메시 로컬 크기 x=120 y=0 z=120, 플레이어 추종. |
| 2026-09-09 | **컷어웨이가 마을에만 걸려 있었다 — 정작 벽이 많은 레이드 맵이 빠졌다.** Stage 0에서 화면공간 컷어웨이(`Spike/OccluderFX`)로 결정했는데 `Safehouse3DLayout`만 그 재질을 쓰고, `Greybox3D`(지역1·고철시장·실내 전부)는 일반 URP/Lit이었다 — 벽·지붕 뒤로 들어가면 플레이어가 그냥 사라진다. `_wall`(지붕도 이걸 쓴다)과 `_car`(1.5m, 앉은 몸을 가린다)를 컷어웨이 재질로 교체. 바리케이드·프롭(1m = 반엄폐)은 애초에 시야를 안 막으므로 그대로 둔다. |
| 2026-09-09 | **`PlayerVision`은 이미 3D였다(확인).** 눈높이 1.5m에서 `Physics.Linecast`로 LOS를 보고, 평면 좌표는 전부 `Plan3D`를 거친다. Stage 2 체크리스트의 "LOS 3D화"는 이미 끝나 있던 항목. |
| 2026-09-09 | **빈 캐시가 굳어 마을 NPC 전원의 하이라이트가 죽어 있었다.** `InteractableObject`가 렌더러 목록을 첫 호출에 한 번만 캐시했는데, 그 첫 호출이 렌더러가 생기기 전에 들어오면 **빈 배열이 그대로 굳어 영영 복구되지 않는다.** 로직은 멀쩡한데 화면에 아무 반응이 없어 원인을 찾는 데 오래 걸렸다(리플렉션으로 캐시를 비우고 다시 부르니 정상 동작). 비어 있으면 다시 훑도록 변경 — 하이라이트는 바라보는 대상이 바뀔 때만 호출되므로 재조회 비용은 무시할 만하다. **교훈: "한 번만 캐시"는 대상이 나중에 생길 수 있으면 조용히 죽는다.** |
| 2026-09-09 | **Stage 4 실측 마감.** 지역1(레이드): 파이프라인 URP-3D / 2D물리 0 / Light2D 0 / 깨진 스크립트 0 / 태양 1개(꼬리표) / 빌보드 262개 / 스프라이트 969→**582**. 남은 582개는 전부 **정당한 2D**다 — 부위 오버레이 270(F1 디버그·빌보드), HP·그로기·말풍선 264(빌보드), 꺼진 앵커 44, 테스트 HP바 2, 투척 데칼 2. 마을(Safehouse3D): 스프라이트 **0개** / 2D물리 0 / Light2D 0 / 태양 1개. |
| 2026-09-09 | **`Vector3`→`Vector2` 암묵 변환 전수 정리(23곳).** 이번 이식의 **최대 실패 유형**이다 — 컴파일도 되고 경고도 없는데 `(x, z)` 대신 **`(x, 높이)`**를 읽어 조용히 틀린 평면에서 계산한다. 투척 착탄점(`ThrowSystem.ClampedLandingPoint`)이 대표적이었고, 그 외 현상 구역 판정(`AnomalyZone.Contains`), 지역 발견·미니맵(`MapZoneVolume`·`NavigationHUD`·`RaidMapUI`), 통로 발견(`PassageMarker`), QA 봇 조작(`QaBot`·`QaSteps`)이 전부 걸려 있었다. 전부 `Plan3D.ToPlan()`을 거치도록 교체. (`GameInput`의 마우스 좌표 1건은 진짜 2D라 제외.) |
| 2026-09-09 | **투척 시스템은 통째로 2D였다.** 던진 돌의 `transform.position = from`(Vector2)이 z 대신 y로 들어가 **돌이 공중/지하로 날아갔고**, 포물선 호도 화면 y 기준이라 3D에선 옆으로 휘었다. 호는 높이(y)·이동은 평면(xz)으로 분리하고 비행 높이 1.0m + 호 1.2m를 줬다. 조준 원과 착탄 표식은 스프라이트 기본 방향이 XY 평면이라 **벽처럼 서 있었다** — x축 +90°로 눕혀 바닥 데칼로 만들고 z-fighting 방지로 0.03m 띄웠다. 돌 자체도 스프라이트 → 상자. |
| 2026-09-09 | **상호작용 하이라이트가 3D에서 통째로 안 보였다.** `InteractableObject.SetHighlight`가 `SpriteRenderer`만 칠했는데 NPC·문·컨테이너가 전부 메시가 됐다 — "이걸 누를 수 있다"는 피드백이 사라지면 무엇과 상호작용되는지 알 방법이 없다. 자식 렌더러를 전부 칠하도록 변경(이름표·게이지 등 빌보드 밑은 제외). 원래 색은 `sharedMaterial`에서 읽는다 — `material`을 읽으면 그 순간 인스턴스화돼 오브젝트마다 머티리얼이 샌다. |
| 2026-09-09 | **Stage 4 착수 — 런타임 감사로 실태 확정.** 코드 grep(2D 물리 17파일·스프라이트 33파일)은 과대평가였다. 플레이 모드에서 실제 씬을 훑으니 **2D 물리 0개 · 깨진 스크립트 0개** — 이식은 이미 끝나 있었고, 남은 것은 `SpriteRenderer` **969개**(적 924 · 월드아이템 28 · 플레이어 15 · 투척 2)와 Systems의 `Light2D` 1개뿐이었다. 셰이더는 전부 Unlit 계열이라 **안 보이진 않지만 조명을 전혀 안 받는 납작한 판**이었다. 이후 작업은 이 목록 순서로 진행. |
| 2026-09-09 | **적 몸 3D화 + 판정·표시 불일치 해소.** `GreyboxLimbs` 스프라이트 6장 → 상자 6개. 옮기면서 드러난 것이 더 크다: 예전 몸은 **원점을 중심으로** 그려져 키가 1m였고 **절반이 바닥에 묻혀** 있었는데, 정작 콜라이더는 발밑부터 1.8m 캡슐이었다 — 맞는 자리와 보이는 자리가 계속 달랐다. y01 0=발끝을 그대로 걸어 땅을 딛고 서게 했다(실측 1.76m). 몸은 앵커(`EnemySprite`)가 아니라 **루트**에 붙인다 — 앵커가 x만 눌린 비균등 스케일(0.8,1,1)이라 3D로 오자마자 폭보다 두꺼운 몸이 나왔다. 덩치는 `SetScale(mul)`로 명시적으로 받는다. 방향은 `flipX` → `SetFacing`(Y축 회전) — 쿼터뷰는 앞뒤도 보인다. `ApplyUnitLook`의 **종류별 식별색이 팔다리에 안 닿아** 모든 적이 같은 색이던 것도 같이 고쳤다. |
| 2026-09-09 | **월드 UI 빌보드(`Billboard` 신설) + 높이 재배치.** 카메라가 55° 내려다보므로 눕혀 둔 판은 찌그러져 게이지도 글자도 안 읽힌다. 대상: HP바·그로기바·이름표·말풍선·퀘스트 마커·부위 오버레이·월드아이템 이름표(이건 **"빌보드 캔버스"라는 이름만 달고 실제로는 고정**이었다). 높이도 전부 1m 몸 기준(HP 0.7 / 이름표 0)이라 **가슴에 박히거나 발치에 깔려** 있었다 → HP 2.02 / 그로기 2.18 / 이름표 2.42 / 말풍선 2.72. |
| 2026-09-09 | **손에 드는 것들 3D화 — 2D에선 한 축이던 것이 3D에선 둘로 갈린다.** 칼·주먹·총 모두 피벗을 **Z축**으로 돌리고 있었다. 3D에서 Z축은 수직면이라 무기가 위아래로 까딱거리기만 하고 스윙이 안 보인다 → **Y축(yaw)**. 평면 방향→yaw 변환도 부호가 뒤집힌다(`atan2(-dir.y, dir.x)`) — 그대로 두면 좌우가 반대로 나간다. 총은 한 걸음 더: **조준(yaw)과 총구 들림·장전 내림(피치)이 2D에선 둘 다 Z라 그냥 더하면 됐는데** 3D에선 갈라진다 → `Euler(0, yaw, pitch)`. 손 높이도 0(=발밑)이라 무기가 바닥을 긁었다 → 칼 1.05 / 주먹 1.02 / 총 1.10m. 주먹의 좌우 벌림은 2D에서 y(화면 상하)였는데 3D에선 **z**다(y로 두면 두 주먹이 위아래로 겹친다). 총알은 회전·길이를 이미 3D로 계산해 두고도 **스프라이트라 local Z 스케일이 무시돼** 길이 없는 점이었다 → 상자. 칼 궤적 셰이더도 빌트인 `Sprites/Default` → URP(분홍 에러 방지). |
| 2026-09-09 | **월드 아이템이 쿼터뷰에서 거의 안 보였다.** 바닥에 눕힌 납작한 스프라이트라 각도상 선으로 사라진다 — 루팅이 핵심 루프인데 떨어진 물건이 눈에 안 띄면 게임이 성립하지 않는다. 지면에 세운 상자(0.28m, 희귀도 색)로 교체. |
| 2026-09-09 | **Systems 씬의 Global Light2D 제거.** URP-3D에서 그 라이트는 3D 메시를 비추지 않아 **아무것도 하지 않는다.** 조명은 이제 맵 씬의 태양(`Lighting3D`가 굽고 `SunLight` 꼬리표가 붙는다) + `RenderSettings` 앰비언트다. `SystemsSceneEnforcer`는 남긴다 — 아직 2D 씬이 열릴 때 떠도는 글로벌을 꺼 주는 역할. 공용 헬퍼 `GreyboxMesh` 신설(큐브 1개 + 색별 머티리얼 공유, 색 변경은 `MaterialPropertyBlock`으로 — `renderer.material.color`는 오브젝트마다 머티리얼을 인스턴스화해 샌다). |
| 2026-09-09 | **자동화를 멈추던 모달이 두 곳 더 남아 있었다.** `SystemsSceneBuilder`와 `GreyboxPaletteBuilder`의 완료 대화상자가 `ContentBuildAll.Quiet`을 안 봤다 — 실제로 Systems 씬 재빌드가 여기서 멈췄고 사람이 눌러야 풀렸다. 특히 팔레트 쪽은 `GreyboxBuild.EnsurePalette()`가 **맵 빌드마다** 부르는 경로라 위험했다. 두 곳 다 Quiet을 보게 수정. (`NpcPrefabBuilder`·`SceneLightingBuilder`·`PlayerMaterialSetup`은 메뉴 전용이라 그대로 둔다.) |
| 2026-09-09 | **URP-3D 파이프라인 전환 — 디스크에 안 써지던 원인은 `File/Save Project` 누락.** 런타임 API·QualitySettings 루프·`GraphicsSettings.asset` 직접 편집까지 세 번 시도해 전부 메모리에서만 맞고 파일은 URP-2D guid를 유지했다. ProjectSettings는 AssetDatabase 바깥이라 `AssetDatabase.SaveAssets()`가 커버하지 않는다 — `EditorUtility.SetDirty(GraphicsSettings.GetGraphicsSettings())` + `EditorApplication.ExecuteMenuItem("File/Save Project")`를 붙이니 즉시 기록됐다. **Unity를 끌 필요가 없었다.** 함께 바꾼 것: `m_TransparencySortMode` 3(CustomAxis)→0(Default), 축 (0,1,0)→(0,0,1) — 3D는 카메라 거리로 정렬한다. `PlayerRig` 카메라의 `m_RendererIndex: 1`→**-1(기본 렌더러를 따름)** — URP-2D는 렌더러가 [Renderer2D, ForwardRenderer] 둘이라 1번을 가리켰지만 URP-3D엔 0번 하나뿐이라 범위를 벗어난다. 고정 인덱스는 파이프라인을 갈아끼우면 이렇게 깨지므로 -1이 맞다. `CameraSortSetup`(2D 정렬축 강제)은 **어디에도 안 붙은 죽은 코드**로 확인 — Stage 5 삭제 대상. **검증(플레이):** 파이프라인=URP-3D / 카메라 ortho size 7 sort=Default rendererIdx=-1 / 화면에 보이는 렌더러 15개. |
| 2026-09-09 | **태양이 둘이었고, 낮밤은 화면을 안 밝히는 쪽을 몰고 있었다.** 맵마다 태양을 굽게 되면서 `PlayerRig`(DontDestroyOnLoad)에 남아 있던 `Sun3D`와 맵의 `Sun3D`가 공존했고, `DayNightCycle.FindSun()`이 "먼저 찾은 디렉셔널"을 쓰는 바람에 **실제로 맵을 밝히는 태양은 밤에도 1.05에 멈춰 있었다.** 값을 재도 몰리는 쪽만 정상으로 보여 전날 검증을 통과해 버렸다. 대응 두 겹: ① `PlayerRig`에서 중복 태양 제거(태양은 맵 씬이 소유) ② `SunLight` 꼬리표 신설 — `Lighting3D`가 구울 때 붙이고, `FindSun`은 꼬리표를 우선하며 후보가 둘 이상이면 경고한다. `followDayNight=false`면 낮밤이 건드리지 않는다(**은신처**는 시간과 무관한 고정 조명이라 여기 해당). |
| 2026-09-09 | **플레이어 허트박스만 아직 2D여서 플레이어가 맞지 않았다.** `PlayerRig/Player/Hurtbox`가 `BoxCollider2D`를 들고 있었다 — `AttackPerformer`는 3D 콜라이더를 스캔하므로 판정이 통째로 비었다. 게다가 2D 콜라이더가 있는 오브젝트엔 3D를 못 붙여(`AddComponent`가 null 반환) 전날 넣은 `Hurtbox`의 기본 박스 폴백이 그 자리에서 NullReference로 터졌다 — **콘솔 에러 1건의 정체가 이것.** 2D 제거 후 `BoxCollider`(0.7×1.6×0.7, center y=0.85, trigger)로 교체. 적 허트박스는 이미 3D였다. |
| 2026-09-08 | **Stage 2 — 3D 조명 성립.** 3D 맵은 **광원이 하나도 없었다**(지역1·고철시장·실내 15씬 모두 0개). 2D 시절엔 `Light2D` 글로벌 하나가 화면 전체를 덮어 맵 빌더가 조명을 만들 이유가 없었고, 그 전제가 3D에서 깨진 채 남아 있었다. 지붕까지 씌운 뒤라 실내는 진짜로 캄캄했다. 조명 셋업을 `Lighting3D` 한 곳으로 모으고(프리셋 Outdoor/Indoor/Hideout — 마을·은신처가 각자 갖고 있던 태양 값을 흡수) `GreyboxBuild.EndScene`이 3D로 구울 때 태양+앰비언트를 얹도록 했다. `Greybox3D.Room`은 방을 만들 때 **천장등을 같이 단다**(9m 간격 격자). 결과: 지역1 태양1+실내등115(방 36), 고철시장 30, 실내 15씬 각 2~5, 마을 8, 은신처 2. **실내등은 안에 들어왔을 때만 켠다** — 점광은 추가 라이트 그림자를 끄고 쓰기 때문에 벽을 뚫고 나가 건물 밖 바닥에 이유 없는 밝은 원을 만든다. `BuildingInterior`가 지붕과 함께 자식 Light를 자동 수집해 껐다 켠다. |
| 2026-09-08 | **낮밤이 3D에서 한 번도 안 돌고 있었다(3중 버그).** ① Systems 씬의 `DayNightCycle.weatherData`가 비어 있어(`fileID: 0`) **WeatherData 분기 전체**(포그·앰비언트·태양 각도)가 죽어 있었다 — 씬을 다시 구울 때마다 손으로 끼우는 참조는 이렇게 조용히 빠진다. `WeatherData.asset`을 `Resources/Data/`로 옮기고(StatDB·GameTuning과 같은 자리) 비었으면 스스로 로드. ② `RenderSettings.ambientLight`는 `ambientMode`가 Flat일 때만 먹는데 맵은 Trilight로 굽는다 — 낮·밤 앰비언트 측정값이 같은 값으로 찍혔다. 색 하나를 sky×1.25 / equator×1 / ground×0.55로 편다. 그리고 `ambientLight`는 별개 슬롯이 아니라 **Trilight의 하늘색과 같은 값**이라 나중에 대입하면 sky를 덮어쓴다. ③ 태양은 맵 씬에 있고 `DayNightCycle`은 Systems에 상주하는데 **씬 전환 훅이 없었다** — 밤에 레이드에 들어가도 T를 누르기 전까지 대낮이었다. `sceneLoaded`에서 캐시를 버리고 다시 건다. **검증(플레이 모드 실측):** 낮 태양 1.00·(50,330,0)·sky 0.438 ↔ 밤 0.18·(60,200,0)·sky 0.050, 실내등 115개 전부 꺼짐(방 36 지붕 덮임). |
| 2026-09-08 | **야간 태양 강도 0 → 0.18(달빛).** `WeatherData`의 `nightIntensity=0` · `nightLightColor=검정`은 2D 시절 값이다 — 그때는 `Light2D` 글로벌이 밤빛을 전담해 디렉셔널을 **일부러 껐다.** 3D에선 그게 곧 완전한 암흑이라, 차가운 달빛(0.18 / RGB 0.55,0.62,0.85)과 반대편 각도(60,200,0)로 바꿨다. 밤 앰비언트(0.04)는 그대로 둔다 — 어둠 자체는 기획 의도(시야 콘·손전등이 봐야 할 몫)이고, 이건 "형체는 보이되 어둡다"는 바닥선만 만든 것. 값은 `Resources/Data/WeatherData.asset`에서 조정. |
| 2026-09-08 | **손전등 3D 스포트 이식.** `Light2D`(Spot)의 `pointLightInner/OuterAngle`·`Radius`를 `innerSpotAngle`·`spotAngle`·`range`로 매핑, `falloffIntensity`는 3D에 대응이 없어 폐기(감쇠는 range가 맡는다). 배터리·F토글·깜빡임 로직은 그대로. **현재 어떤 프리팹·씬에도 붙어 있지 않은 휴면 코드**라 배선은 없다 — 되살릴 때 PlayerRig에 `flashlightPivot`(3D 회전)을 다시 만들어야 한다. |
| 2026-09-08 | **별도 실내 씬 폐기 — 건물을 맵 안에서 걸어 들어가게 + 맵 1.7배.** 지역1·고철시장 건물이 `Int_*` 씬으로 전환하는 대신 **같은 맵 안의 방**이 됐다(`Greybox3D.Room` = 바닥+벽4(문 갭)+지붕+`BuildingInterior` 지붕 off). 씬 전환 문 13 → 1(맨홀 탈출구만 유지). **크기 문제 실측**: 별도 실내 씬은 12×9~22×18m인데 건물 상한 `MaxSpan=13m`이라 실내가 최대 11m — 절반이었다. 상한의 근거는 코드 주석에 남은 **2D 스프라이트 아트 제약**("너무 큰 건물은 리소스상 불가능")이고, 3D 그레이박스엔 해당하지 않는데 상한만 남아 있었다. 좌표가 빌더에 상수로 박혀 있어(지역1 2223줄) 프리미티브 계층에 **평면 배율**(`Greybox3D.PlanScale`)을 넣어 맵째로 키웠다 — **1.7배**: 건물 13→22m(실내 20.5m), 맵 160×168→**269×282m**. 높이는 곱하지 않는다(벽 3m·사람 1.8m는 이미 실제 치수). 대가: 도보 시간도 1.7배 — 레이드가 늘어지면 배율을 1.4~1.5로 낮추거나 건물 수를 줄인다. 실내 최소 발자국 7m 미만은 방으로 만들지 않는다(사람이 낀다). |
| 2026-09-08 | **콘솔 에러 2종 해소(둘 다 3D 이식 부작용).** ① `UIManager`의 UI 스택이 반복 중 인덱스로 지워 `ArgumentOutOfRange` — `IsOpen()`·`Close()`가 내부에서 `PopUI`를 불러 스택을 줄이면 들고 있던 i가 범위를 벗어난다. 스냅샷을 돌며 **이름으로** 지우도록 변경(멱등). ② `Hurtbox`의 `[RequireComponent(typeof(Collider))]`는 **추상 타입이라 자동 추가가 안 된다** — 2D 시절엔 프리팹에 이미 붙어 있어 드러나지 않았다. 없으면 기본 박스를 붙이고 경고하도록. |
| 2026-09-08 | **Stage 3 맵 변환 완료 — 2D 잔재 0.** 지역1 메시 707 / 3D콜 709 / **스프라이트 0 / 2D콜 0** / 스폰 9, 고철시장 메시 56 / 3D콜 60 / 0 / 0 / 스폰 4, 실내 15씬 동일. 마커까지 마무리하며 드러난 것: 각 빌더가 `Marker`·`Note`·`Enter`·`Exit`·`Poi`를 **자기 파일 안에 복제**해 갖고 있어서, 프리미티브 스위치만으로는 안 잡혔다(같은 함정에 헬퍼 수만큼 걸린다). `QuestPoiZone`은 `[RequireComponent(Collider2D)]`라 3D에서 `AddComponent`가 null을 반환 → 컴포넌트 자체를 3D로 이식. 문은 3D에서 `BuildingEntrance`(BoxCollider2D 전제) 대신 `InteractableObject.ExitPoint`로 씬 전환을 직접 건다. |
| 2026-09-08 | **Stage 3 — 고철시장·지역1 3D 변환(기하 완료, 마커 일부 잔여).** 지역1 메시 711 / 3D콜라이더 663 / 최고Y 69, 고철시장 메시 60 / 3D콜 45. 벽·바닥·건물·잔해 등 **기하는 전부 3D**로 옮겨졌다. 옮기면서 드러난 것: ① 빌더마다 `GreyboxBuild`를 안 쓰고 **자체 프리미티브 헬퍼**를 갖고 있다(고철시장 `Bar`/`Floor`, 지역1은 공용 사용) — 스위치를 그쪽에도 달아야 한다. ② `Blocker`(막힌 통로)가 세 빌더에 각각 있고 전부 3D 상자에 `BoxCollider2D`를 붙이려다 `AddComponent`가 null을 반환해 죽었다. ③ 마커(`gb_crate`·`gb_note`·`gb_enemy`·`gb_exit`)는 그레이박스 상자로 대체. **잔여**: 고철시장 스프라이트 15 · 지역1 17 — 각 빌더의 자체 스폰/출입구 헬퍼(2D 좌표 `(x,y,0)`로 배치)라 3D 분기가 더 필요하다. |
| 2026-09-08 | **Stage 3 — 맵 빌더 3D화(실내 15씬 완료).** 맵 빌더가 전부 `GreyboxBuild` 위에 서 있다는 점을 이용해, 빌더를 고치는 대신 **프리미티브 계층만** 3D로 바꿨다(`Greybox3D` + `GreyboxBuild.Use3D` 스위치). 바닥 세 개(`Bar`·`Floor`·`Marker`)만 갈아끼우니 `Wall`·`Barricade`·`Car`·`Prop`과 그 위의 `WallSeg`·`WallLine`·`Building`·`RotBuilding`이 전부 따라온다 — **지역1 2223줄·고철시장 500줄·실내 452줄을 한 줄도 안 고치고** 3D 맵이 나온다. 기본값 false라 2D 경로도 살아 있어 비교 가능. 실내 15씬 전부 3D 재생성(스프라이트 0, 2D 콜라이더 0). 높이는 종류별 기본값(실내 벽 2.6m / 거리 벽 3.0m / 바리케이드 1.0m = 반엄폐 / 차량 1.5m). |
| 2026-09-08 | **일괄 빌드를 막던 모달 3종 해소.** ① `GreyboxBuild.EndScene`의 "생성 완료" 창이 `!isBatchMode`만 보고 `ContentBuildAll.Quiet`을 안 봐서 **씬마다** 멈췄다. ② `EditorSceneBuildUtil`의 "무제 씬" 창도 같은 이유. ③ `InteriorBuild`의 출구·게이트가 3D 상자에 `BoxCollider2D`를 붙이려다 `AddComponent`가 null을 반환해 NullReference(보석상·경찰서 빌드 사망) — 3D 경로(`BoxCollider` + `SceneDoor3D`) 추가. **일괄 빌드가 원래부터 불가능했고**, 15씬을 돌리려다 드러났다. |
| 2026-09-08 | **Stage 3 착수 — 프롭 카탈로그 3D판.** `Prop3DDefinition` + `Prop3DBuilder` 신설(2D판 615줄 대체). 2D의 스프라이트·정렬 순서가 빠지고(깊이는 카메라가 푼다) **높이**와 **엄폐(Cover)** 분류가 들어왔다. 콜라이더 모드 None/Box/Mesh(볼록)/Capsule/Composite. **모델이 없어도 선다** — `mesh`가 비면 `size` 크기 상자로 대신 세우고, 콜라이더·높이·엄폐 판정은 그레이박스에서도 진짜와 똑같이 동작한다(프랍 63종이 나오기 전에 레이드 맵을 세우려면 이 길뿐). 구조는 루트(콜라이더·파괴) + 자식 `Visual`(메시) — 바닥 정렬을 루트 위치로 처리하면 호출자가 위치를 지정하는 순간 덮어써진다. 메시 콜라이더만 메시와 같은 트랜스폼에 붙인다(루트에 붙이면 자식 오프셋·스케일이 반영 안 됨). |
| 2026-09-07 | 3D 쿼터뷰 전환 결정 4건(완전 3D / 오소 고정 쿼터뷰 / 단차·엄폐 / 아트 전부 3D) + 실측 + Stage 0~5 계획 수립. 미착수. |
| 2026-09-07 | **카메라 재결정 + 컷어웨이 구현.** yaw 45°→**0°**(대각선 폐기), pitch 50°→**55°**, ortho 10→**7**. pitch를 눕혀도 오클루전은 그대로 치명적임을 재확인. 컷어웨이 셰이더에 **깊이 조건**(뒤 건물 보호)과 **전역 유니폼·해상도 비의존 반경** 반영. |
| 2026-09-07 | **Stage 0 완료.** 샌드박스에서 카메라 pitch 50°/yaw 45°/ortho 10 + 캐릭터 스케일 1.0 확정, 오클루전 4종 비교 후 **화면 공간 컷어웨이** 채택. 함정 3건 기록(그림자 거리·컷어웨이 판정축·캐릭터 가독성). |
| 2026-09-07 | **Unity 6.6(6000.6) 업그레이드 결정** — 사용자가 LTS 아니어도 무방 판단. 그 과정에서 Spine 4.2 소스가 `Object.GetInstanceID()` obsolete-as-error(CS0619)로 컴파일 불가 → **Stage 4의 Spine 제거를 앞당겨 실행**(689파일·cha 에셋·SpinePlayerSetup·SpineLitURP·PlayerRig 노드·asmdef 참조). 상태→모션 매핑과 HitFlash/InjuryVFX 구멍은 Stage 4 항목으로 이월 기록. |
