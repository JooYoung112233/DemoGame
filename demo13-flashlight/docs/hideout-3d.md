# 은신처 3D — 공간 규격 · 소품 정의서

> 3D 쿼터뷰 전환의 **첫 실제 공간**. 2026-09-08 그레이박스 → 2026-09-11 원화 기준 아트 개선.
> 실제 씬: `Assets/Scenes/Hideout.unity`. 과거 그레이박스 빌더는 `Assets/Editor/Hideout3DLayout.cs`.
> 아트 제작: `tools/build_hideout02.py` + `build_hideout_textures.py`; Unity 임포트/배치는 Editor API를 사용한다.
>
> 좌표 규약: 월드 = **XZ 평면**, 위 = **+Y**, **1u = 1m**. 2D판 좌표 `(x, y)`를 `(x, 0, y)`로 그대로 옮겨
> 시설 배치가 [2D판](safehouse.md)과 1:1로 대응한다. 관련: [3d-migration.md](3d-migration.md)

## 결정

### 2026-09-12 — 퇴장 후 이동 상태 복구 버그 수정

- 이동 검수 중 Hideout→Safehouse 이후에도 `TopDownPlayer.enabled=false`, Rigidbody의 `isKinematic=true`가 남는 문제를 확인했다. 디오라마에서 잠근 이동·물리·상호작용 상태를 저장하고 씬 언로드 시 원래 값으로 복구한다.
- Unity MCP 왕복 검증: Hideout에서 이동/상호작용 비활성·kinematic=true, 마을 복귀 후 둘 다 활성·kinematic=false. 이전 상태를 보존하며 무조건 켜는 방식은 사용하지 않는다. `ArtWork/MovementPace/hideout-state.json`, `returned-town-state.json`에 기록.

### 2026-09-12 — 시설 UI 뒤 딤과 좌측 공간 확대

- 사용자 요청: “하이드아웃 우측 UI 뒤 약간 딤처리”, “좌측 맵을 지금보다 약 2배 가까이”.
- 추가 사용자 결정: “애초에 하이드아웃 처음에도 너무 멀다.” 최초 진입·시설 창을 닫은 기본 화면도 기존 대비 약 2배 확대한다. 시설 창에서는 같은 확대 배율로 선택한 시설·캐릭터 주변에 중심을 맞춘다. 우측 시설 도크 뒤에 약한 어두운 배경을 추가하며 다른 시설은 상단 버튼으로 바로 선택한다.
- 반영값: `GameTuning.hideoutViewZoom=2`, `hideoutDockDimAlpha=0.32`. 우측 도크 폭+우측 여백+24px 영역에 검정 딤을 두고, 딤은 클릭을 가로채지 않는다. 확대 중에는 상단 180px·하단 112px의 배경을 가려 기존 제목/시설/출구 버튼을 읽기 쉽게 유지한다.
- 검수 기준: 최초 진입·닫힘·시설 선택 모두 기존 오소 6.44865의 절반인 약 3.22433을 사용한다. 7개 시설의 캐릭터 위치와 UI 조작, 창을 닫을 때 확대 유지·딤 숨김을 확인한다. 검수 캡처와 좌표는 `ArtWork/HideoutDockCloseup`에 보존한다. 확대에 따라 방 외곽 일부는 화면 밖으로 나가며 시설 바로가기는 계속 사용할 수 있다.
- 검수 완료: Unity MCP 재컴파일 성공, 실제 씬 진입 시 오소 3.224326·7시설 및 닫힘 3.224325 측정. 시설 버튼 7개와 닫기 버튼의 EventSystem 입력 통과, 딤 raycast 비활성·닫힘 시 숨김 확인. 콘솔 오류 0건. 자체 검수 완료이며 사용자 최종 시각 승인은 별도다.

### 2026-09-11 — 인게임 비율·조명·생활감 개선 승인

- 질문·제안: 캐릭터/가구 상대 비율, 따뜻한 국소 조명, 생활 소품, 방을 가리는 UI를 순서대로 보완할지?
- 사용자 결정: **“수정해줘” — 인게임 시안 평가에서 제안한 개선을 진행한다.**
- 적용 범위: 하이드아웃 전용 표현과 프레이밍. 캐릭터의 전역 크기·전투 수치·인벤토리 착용 및 달릴 때 수납 규칙은 유지한다. 구체적인 시각 수치는 구현 후 비교 시안으로 확인한다.
- 반영값: 하이드아웃 캐릭터와 별도 근접무기 비주얼 배율 0.84, 임시 칼날 색 강도 0.65. 착용 랜턴은 켠 상태로 광량만 0.15배(몸 보조광 0.9→0.135) 적용하며 씬 종료 시 원복한다. 이동이 비활성인 디오라마에서도 장비 대기 모션과 무기 방향을 갱신한다.
- 공간: 선반 수납함/통조림, 작업대 메모, 접은 천, 보관 붕대, 하단 상자와 작업대 광원을 추가했다. `LivedInDetails` 18개 자식, 전체 MeshRenderer 95개. 장식 충돌체는 비활성으로 시설 접근 공간을 유지한다.
- 조명: 하이드아웃 Sun3D 강도 0.95/색 (1,0.89,0.76), Bulb 0.45, 침대 랜턴 0.25, 작업대 따뜻한 점광원 0.55/범위 3.2m. Systems 전역 조명값은 바꾸지 않는다.
- UI·카메라: 상단 메뉴와 하단 112px를 제외하고 실제 메시 외곽에 6% 여백을 두어 방을 담는다. 중복 우상단 나가기 버튼을 제거하고 기존 하단 나가기·인벤토리·퀵슬롯은 유지한다. 초기화 중 교체된 카메라에 포커스를 재연결하며 화면 크기/카메라 회전이 바뀌면 재계산한다.
- 검증: MCP 실제 플레이에서 작업대 패널 열림/방 좌측 프레이밍 확인. 저장 씬 시설 9개/스크립트 누락 0/서는 공간 충돌 0/UV·노멀·탄젠트 통과. 방의 뷰포트 외곽 (0.30,0.12)~(0.70,0.81)이 UI 제외 영역 안에 들어옴을 수치 확인했다. 씬 언로드 후 몸·무기 배율 1과 몸 보조광 0.9 복원, 재진입 재적용 확인. 콘솔 오류 0건. `InGame.png`, `InGameWorkbench.png`, `PresentationValidation.json`, `FramingValidation.json`에 결과 보존.

### 2026-09-11 — 인게임 시안 평가 질문

- 사용자 질문: “너가 보기엔 어떠니” — 실제 인게임 하이드아웃 화면의 품질 평가 요청.
- 평가: 이전보다 목재·금속·침구가 차분해지고 쿼터뷰 공간이 잘 읽힌다. 다만 캐릭터의 큰 실루엣·밝은 머리·돌출된 무기가 가구보다 시선을 끌고, 원화에 비해 선반·작업대의 생활 소품과 국소 마모가 적어 새 가구 전시장처럼 보이는 부분이 있다. 인게임 조명은 원화보다 차가우며 상단 시설 바와 하단 HUD가 방 외곽에 겹친다.
- 제안(사용자 미확정): 먼저 캐릭터와 가구의 상대 비율·무기 표시를 점검하고, 하이드아웃 조명의 명암·색온도를 조정한 뒤 선택적으로 생활 소품과 마모를 보강한다. 평가 요청만으로 캐릭터 크기·무기 수납 규칙·UI·조명을 변경하지 않는다.

### 2026-09-11 — 수정 질감 인게임 적용

- 질문·확인: 수정한 쿼터뷰 하이드아웃 시안을 실제 게임에 적용할지?
- 사용자 결정: **“좋다 이느낌 인게임에 넣고 보자”** — 수정한 목재·금속·침구 표현을 실제 하이드아웃에 적용하고 게임 화면으로 확인한다.
- 구현 기준: MCP로 저장 씬과 런타임을 확인하고 기존 시설 기능·쿼터뷰를 유지한다. 인게임 조명 아래 가독성을 확인한다.
- 확인 결과: 저장된 Hideout02 연결을 그대로 사용해 Systems + Hideout 실제 Play Mode로 진입했다. 새 침구·표면 맵과 캐릭터·시설 바·이름표가 함께 출력되는 `ArtWork/HideoutReview/InGame.png`를 MCP `capture_game_view(source=screen)`으로 저장했다. 확인 시점 콘솔 오류 0건. 기존 인게임 조명은 검토 렌더보다 어둡고 차가운 편이다. 이후 플레이 상태가 바뀌어 작업대 버튼 검증은 완료하지 못했으며, 시설 패널 동작까지 통과한 것으로 간주하지 않는다.

### 2026-09-11 — 질감 수정과 셰이더 작업 순서 검토

- 사용자 질문(선택지): 지금 질감을 수정할지, 셰이더를 나중에 작업할지?
- 사용자 결정: **“수정해줘 그럼” — 대표 재질부터 수정하는 순서로 진행 승인**. 쿼터뷰와 검토 조명을 유지하고 바닥·철판·침구를 우선 수정한다.
- 제안: 현재 URP Lit에서 조명·색감·거칠기의 기본 기준을 고정하고, 바닥 목재·철판·침구 세 가지 대표 재질의 질감과 필요한 형태를 먼저 수정한다. 실제 게임 쿼터뷰 크기에서 비교·컨펌한 뒤 다른 소품으로 확장한다. 전용 스타일 셰이더는 그 다음에 검토한다.
- 근거: 현재의 반복 나뭇결·구름형 금속 얼룩은 표면 맵의 문제이고, 판처럼 보이는 침구는 형태도 원인이다. 후속 셰이더만으로 해결될 것으로 전제하지 않는다. 반대로 전체 소품의 텍스처를 먼저 완성하면 표현 방향 변경 시 재작업이 커진다.
- 반영: 목재 결 대비와 규칙성을 낮추고, 도장 철판의 넓은 얼룩·금속성·반사를 줄였다. 색 변화와 미세 표면 노멀을 분리했다. 매트리스 모서리와 베개를 둥글게 만들고 담요에 접힘·처짐·기운 자국을 넣었으며 매트리스 관통을 보정했다. 공유 맵을 사용하는 가구에도 같은 수정이 적용된다. 카메라·조명·URP Lit은 유지했다. 수정 결과는 사용자 컨펌 전이다.
- 작업 방식: 사용자 지시에 따라 Unity 임포트·검증·렌더를 MCP로 수행한다. 컴퓨터 화면 제어는 사용하지 않는다.

### 2026-09-11 — 1차 시안 질감 피드백·쿼터뷰 재확인

- 사용자 질문: “질감이 이상한데 어떻게 보이는가? 우리는 쿼터뷰로 가기로 했다.”
- 사용자 결정 재확인: **기존 3D 쿼터뷰 방향을 유지한다.** 원화의 배치와 분위기를 참고하되 정면에 가까운 구도로 바꾸지 않는다. 1차 질감은 승인된 상태가 아니다.
- 진단: 규칙적인 고대비 나뭇결, 금속에 넓게 퍼진 구름형 얼룩, 각진 침구와 평평한 담요가 원화의 절제된 손그림 표면과 어긋난다. 여러 재질에 비슷한 노이즈를 적용한 것이 원인이다.
- 교정: 게임 카메라 pitch 55°를 유지하고 방 회전을 이전 쿼터뷰 배치인 215°로 복구한다. 검토 렌더도 이 상대 각도에 맞추고 방 모서리가 잘리지 않도록 프레이밍한다. 재질 재작업은 큰 색면·낮은 대비·재질별 마모 위치를 기준으로 한다.

### 2026-09-11 — 하이드아웃 내부 원화 지정

- 질문·확인: 사용자가 `Assets/GPT/안전구역/주인공집/ChatGPT Image 2026년 6월 10일 오후 06_13_26.png`를 첨부하고 “이건 하이드 아웃”이라고 지정했다.
- 사용자 결정: 해당 이미지를 **하이드아웃 내부 아트 기준**으로 분류한다. 앞서 지정된 `Assets/GPT/안전구역/ChatGPT Image 2026년 6월 11일 오후 02_52_23.png`는 안전구역 외부 전체 기준이다. 원화는 `Assets/GPT`를 우선 확인한다.
- 원화에서 확인한 구성: 어두운 세로 골판 철판 벽과 코너 프레임, 갈색 목재 마루, 하단 중앙 출입문과 중앙 러그. 화면 위쪽에 침대·수납 선반, 오른쪽에 작업대/공구와 의료대, 왼쪽에 사물함·라디오/발전기·요리대가 배치돼 중앙 통로가 비어 있다.
- 표현 기준: 금속·목재·천의 구분, 모서리 마모·얼룩·봉제/패임, 가구 주변의 접촉 그림자와 따뜻하고 어두운 생활 공간 분위기를 함께 비교한다. 현재 단색 3D 소품이 원화 품질에 도달했다고 보지 않는다.
- 후속 사용자 결정(2026-09-11): “하이드아웃 먼저 해보자.” 해당 내부 원화를 기준으로 하이드아웃 아트 개선에 착수한다. 철판 벽·목재 바닥·시설 가구의 형태/UV/텍스처/노멀·거칠기와 조명을 함께 다듬고 실제 Unity 렌더로 비교한다. 기존 시설 기능과 디오라마 연결을 보존하며 전투 작업과 별개로 진행한다.
- 구현 초안(컨펌 전): 원화의 정사각형 배치와 시설 간 여백을 살리기 위해 실내를 6×6m로 구성한다. 뒤쪽 철판 벽은 남기고 나머지 세 면은 낮은 단면으로 표현한다. 위쪽 침대/선반, 우측 작업/의료, 좌측 라디오/발전기/요리, 중앙 러그와 하단 중앙 문을 배치한다. 이 수치·표현은 원화 해석에 따른 첫 시안이며 사용자 확정 수치가 아니다.

### 기존 공간·상호작용 결정

| 날짜 | 질문 | 결정 |
|---|---|---|
| 2026-09-08 | 3D 전환의 첫 공간을 무엇으로? | **은신처**. 샌드박스 테스트 씬은 폐기. `safehouse.md`의 "3D 은신처는 캐릭터 확인 이후" 계획대로 |
| 2026-09-08 | 실내에 천장을 만들 것인가? | **만들지 않는다.** pitch 55° 쿼터뷰에서 지붕이 있으면 내부가 아예 안 보인다. "안에 있다"는 느낌은 **4면 벽 + 어두운 조명 + 화면 가장자리 암전**이 낸다 |

## 상호작용 — 디오라마 (2026-09-08 확정)

은신처는 **걸어다니지 않는다.** 2D판의 타르코프식 클릭 방식을 유지한다.
다만 2D판처럼 캐릭터를 **숨기지 않는다** — 클릭한 시설로 캐릭터가 가서 자세를 잡는 **디오라마**다.

| 시설 | 캐릭터 자세 | UI 자리 |
|---|---|---|
| (대기) 의자 | **앉아 있음** | — |
| 침대 | **눕는다** | **하단** — 화면을 가리지 않는 바 또는 시간 표기 |
| 요리대 | 앞에 선다 | **우측 패널** |
| 작업대·의료대·창고·라디오·파견·발전기 | 앞에 선다 | **우측 패널** |

### 원칙: UI가 캐릭터를 가리지 않는다
캐릭터가 시설에서 자세를 잡는 것이 이 화면의 요점이므로, 그 위에 패널을 덮으면 의미가 없다.

- UI는 **우측 도크**(840×960, 전 시설 동일)로 붙인다. 전체화면 패널 금지.
  시설마다 다른 자리에 띄우면 화면 크기가 들쭉날쭉해 산만하다 — **전 시설 동일**이다.
  (`Dock.Bottom`은 코드에 남아 있지만 쓰는 시설이 없다. 짧은 상태 표시가 필요해지면 그때 쓴다.)
- 도크 안: **좌상단 시설 이름 · 우상단 닫기 X · 나머지는 내용이 꽉 채운다.**
- 시설을 누르면 **카메라가 부드럽게 이동해** 방을 도크 반대쪽으로 밀고, 그 영역에 맞게 줌아웃한다.
  순간이동이 아니라 스무딩 이동이다.
- 화면 상단에 **시설 바로가기 바**가 있다. 소품 클릭과 완전히 같은 동작이다.

### 어디를 고치면 되는가 (파일 지도)

한 파일이 커지면 "이 화면 좀 고쳐줘"에서 고칠 자리를 찾는 데만 시간이 든다.
**기능 단위로 `partial`을 나눠 뒀다** — 아래 표에서 바로 찾아 열면 된다.

| 고치고 싶은 것 | 파일 |
|---|---|
| 카메라 확대·축소, 방을 화면 어디에 담을지 | `HideoutDiorama.Camera.cs` (수치는 Control Panel ▸ 은신처 화면) |
| 상단 시설 바로가기 바 (버튼·순서·색) | `HideoutDiorama.Bar.cs` |
| 캐릭터가 서는 자리·바라보는 방향·자세 | `HideoutDiorama.Pose.cs` |
| 클릭 판정, 무엇을 눌렀을 때의 전체 흐름 | `HideoutDiorama.cs` |
| 기존 UI 탐지(방금 켜진 패널 찾기), 시설 설명 문구 | `HideoutDiorama.Panels.cs` |
| 도크 크기·여백·상단 띠, 방이 쓸 화면 영역 | `HideoutDockPanel.Layout.cs` |
| 시설별 기능 버튼 이름 / 눌렀을 때 열리는 UI | `HideoutDockPanel.Facility.cs` |
| 도크 프레임 모양(배경·제목·닫기 X·버튼) | `HideoutDockPanel.Build.cs` |
| 남의 패널을 도크에 앉히고 되돌리기 | `HideoutDockPanel.Adopt.cs` |
| **"창고에 장비창이 같이 나온다", "제목이 두 번 나온다"** | `DockedLayout.Rules.cs` |
| 다단→단일 컬럼 쌓기, 축소/스크롤 판단 | `DockedLayout.Ops.cs` |
| 되돌리기(원상복구) 규칙 | `DockedLayout.Session.cs` |

수치는 **Control Panel(`Tools ▸ TopDown ▸ Control Panel`)의 "은신처 화면"** 항목에서
만진다 — 카메라 오소, 도크 폭·높이·여백, 축소 하한. 코드를 고치고 재컴파일할 필요가 없다.
(`GameTuning.asset`이 정본이고, 없으면 각 파일의 기본값으로 떨어진다.)

### 구현

| 구성 | 파일 | 역할 |
|---|---|---|
| **오케스트레이터** | `Scripts/TopDown/HideoutDiorama.cs` | 시설 클릭(3D 레이캐스트) → 캐릭터 이동·시설 응시·자세 → 카메라 포커스 → 기존 UI 열기. 씬에 있으면 `HideoutController`가 **캐릭터 숨김·카메라 고정·2D 클릭 판정을 양보**한다(`HideoutDiorama.Active`) |
| **역할 라벨** | `Scripts/TopDown/FacilityLabel.cs` | 소품 머리 위에 "침대·작업대·창고…"를 **상시 표시**. 걸어다니지 않는 화면이라 근접 E 프롬프트가 성립하지 않는다 — 무엇을 누를 수 있는지 한눈에 보여야 한다. `TextMesh`+`LegacyRuntime.ttf`, 카메라 향해 빌보드 |
| 시설 앵커 | `Scripts/TopDown/HideoutFacilityAnchor.cs` | 시설별 **캐릭터가 설 자리·자세(Stand/Sit/Lie)·UI 도킹(Right/Bottom/None)**. `moduleKey`는 `HideoutUI.Show(module)` 값과 일치해야 한다 |
| 카메라 연출 | `CameraFollow.SetFocus(focus, screenBias, orthoSize)` | 피사체를 화면 비율만큼 밀어 UI 자리를 비운다. `ClearFocus()`로 복귀. 도킹에서 편향 자동 산출(우측→캐릭터 왼쪽, 하단→캐릭터 위) |

### 현재 자세 범위

시설 9곳의 `moduleKey`, 상호작용 타입, 도킹 설정을 유지한다. 앉기·눕기 전용 모션은 이번 아트 패스의 범위가 아니며 현재 앵커의 서기(Stand) 설정을 보존한다. 기존 패널 도킹과 기능 버튼은 이미 구현되어 있다.

## 현재 아트 시안 — Hideout02 (2026-09-11)

| 항목 | 적용 값 |
|---|---|
| 저장 씬 / 아트 루트 | `Assets/Scenes/Hideout.unity` / `Map/Environment/Structures/Hideout02_Placed` |
| 실내 | **6×6m**, 중심 `(7,0,4.5)`, Y 회전 **215°** — 고정 게임 카메라 pitch 55°에서 쿼터뷰로 표현 |
| 벽 | 뒤쪽 골판 철판 약 2.1m; 나머지 세 면 낮은 단면. 벽 충돌체 높이 2.4m |
| 바닥 | 가로 방향 목재 판재, 엇갈린 이음새와 못. 중앙 직조 러그 |
| 출입문 | 하단 중앙 양문 단면 모델. 문 열기/출입 애니메이션은 미구현 |
| 카메라 | 포커스 `(7,0.52,4.5)` 기준 실제 메시 외곽에 6% 여백을 두고 상단 메뉴·하단 112px·시설 도크를 피하도록 자동 프레이밍. 기존 오소/점유 반경 수치는 메시가 없는 경우의 폴백 |
| 조명 | 따뜻한 Sun3D 0.95, 전구 0.45/범위 8, 침대 랜턴 0.25/범위 2.8, 작업대 점광원 0.55/범위 3.2. 착용 랜턴은 실내에서 광량만 0.15배 |
| 아트 리소스 | `Assets/Art/Environments/Hideout02/Hideout02_Interior.prefab`, 개별 프리팹 18종, 배치 32개 |
| 표면 | 1024px 타일형 컬러·탄젠트 노멀·Metallic/Smoothness 맵 5세트, URP Lit 재질 22종 |
| UV·노멀 | 1 UV 단위=1m 방향성 투영, 목재 긴 축 결 정렬. 법선/탄젠트/UV0 임포트 검증, Unity 보조 UV 생성 |
| 구조 | 원본 Blender는 편집 가능한 부품 유지, Unity는 소품 단위 정적 메시 결합. 첫 시안 70개에 생활 소품을 더해 활성 MeshRenderer 95개 (기존 승인 창고 상자 포함) |

### 시설 연결

위치는 새 씬에 저장된 **월드 좌표**. 수치는 이번 원화 시안의 구현값이며 최종 사용자 컨펌 전이다.

| 모듈 | 모델 | 서는 위치 (X,Y,Z) |
|---|---|---|
| `bed` | 목제 침대 `Cot01` | (7.70,0,5.33) |
| `dispatch` | `MapBoard01` | (9.51,0,5.07) |
| `workbench` | 공구·바이스 `Workbench01` | (8.76,0,4.19) |
| `idle` | 목제 `Stool01` | (7.63,0,4.09) |
| `medical` | 의료대 `MedicalStation02` | (7.47,0,2.39) |
| `radio` | `RadioTable02`와 별도 라디오 모델 | (5.55,0,5.32) |
| `generator` | `Generator01` | (5.01,0,4.55) |
| `cooking` | 버너·냄비·도마 `CookingStation02` | (4.56,0,4.07) |
| `stash` | 기존 승인 `StorageChest01` 그대로 재사용 | (6.12,0,5.07) |

### 재생성 / 확인

1. `tools/build_hideout_textures.py` 실행(Python + NumPy/Pillow).
2. Blender 백그라운드에서 `tools/build_hideout02.py` 실행. `hideout02_blender_materials.py`로 Blender에도 같은 표면 맵 연결.
3. Unity MCP `eval_file`로 `tools/setup_hideout02.cs` 실행 → 임포트/프리팹 생성. `tools/unity_mcp_call.py`는 공식 `unity mcp --project-path` stdio 서버에 연결하는 클라이언트다.
4. 편집 모드에서 `tools/place_hideout02.cs` 실행 → 기존 시설 데이터를 복사하고 하이드아웃만 저장. 다시 실행해도 시설이 중복되지 않는다. 열린 하이드아웃에 저장하지 않은 변경이 있으면 중단한다.
5. `tools/render_hideout02.cs` → `ArtWork/HideoutReview/After.png`, `QuarterView.png`, `Detail.png`. **실제 Unity 모델의 검토용 조명 렌더**이며 플레이 중 UI 화면은 아니다.

6. `tools/polish_hideout02.cs`를 MCP로 실행 → 기존 시설 앵커를 유지하며 프리팹/저장 씬의 생활 소품과 실내 조명을 재적용한다. 전체 빌더·배치 스크립트를 다시 실행했으면 이 단계도 실행한다. 마지막으로 실제 Play Mode에서 `capture_game_view(source=screen)`을 사용해 UI 포함 화면을 확인한다.

침구만 갱신할 때는 Blender 백그라운드에서 `tools/refine_hideout02_surfaces.py`를 실행한다. `hideout02_bedding.py`는 전체 빌더와 공유하며 편집용 Blender 원본·침대 FBX·매니페스트를 함께 갱신한다. 이후 MCP로 `Cot01.fbx`를 임포트하고 검증/렌더한다. `MaterialBefore.png`와 `MaterialBeforeDetail.png`는 이번 질감 수정 전의 같은 카메라·조명 비교본이다.

검증 산출물은 같은 아트 폴더의 `ImportValidation.json`, `SceneValidation.json`, `SavedSceneValidation.json`에 남긴다. `tools/validate_hideout02.cs`로 시설 9종/키, 누락 스크립트 0, 포커스 참조, 반경 0.28m 캐릭터 공간(높이별 구 샘플)과 가구 충돌 0, 메시 UV·법선·탄젠트를 확인했다. 좁았던 파견·대기·조리 지점은 안쪽으로 이동했다. 이번 패스는 런타임 전투 코드·다른 맵을 수정하지 않는다.

### 남은 확인

- 원화의 손그림 윤곽·생활 흔적 밀도를 완전히 재현한 최종 아트는 아니다. 질감과 침구 형태를 수정한 시안으로 컨펌한다.
- 플레이 상태에서 시설별 UI·캐릭터 가독성은 별도 실제 플레이 검증이 필요하다. 이번 확인은 저장 씬과 에셋의 배선·공간·렌더 검사다.
- 외부 컨테이너와 실내 크기의 대응, 문 개폐 및 출입 연출은 후속 결정 사항이다.

## 변경 로그

- 2026-09-12: 사용자 요청으로 우측 시설 UI 뒤 32% 딤과 2배 확대를 추가했다. 후속 피드백에 따라 최초 진입·창 닫힘에도 2배 확대를 유지하도록 범위를 넓혔다. 확대된 배경과 상하단 UI의 겹침을 가린다.

| 날짜 | 내용 |
|---|---|
| 2026-09-11 | 사용자 승인으로 인게임 비율·따뜻한 조명·생활 소품·UI 여백 개선. 몸/별도 무기 0.84배와 착용등 0.15배는 하이드아웃에만 적용 후 복원. 실제 외곽 프레이밍과 초기 카메라 교체 대응, 작업대 UI·씬 종료 복원 검증 완료. |
| 2026-09-11 | 수정 질감 시안의 인게임 적용 승인. MCP로 Systems + Hideout 플레이·UI 포함 게임 화면 캡처, 콘솔 오류 0건 확인. 저장 씬 연결 유지. |
| 2026-09-11 | 사용자 질감 수정 승인 반영: 목재 반복 결/금속 얼룩/천 노이즈 완화, 미세 노멀 분리, 둥근 침구와 담요 처짐·접힘·기운 자국 구현. 쿼터뷰·조명 유지. Unity MCP로 임포트·시설 9종/표면 채널/공간 검증·렌더 완료. 수정 시안 컨펌 대기. |
| 2026-09-11 | 질감 수정/셰이더 작업 순서 질문 기록. 대표 재질 3종을 기본 조명 아래 쿼터뷰에서 검증한 뒤 확장하고 전용 스타일 셰이더를 검토하는 순서를 제안. 사용자 확정 전. |
| 2026-09-11 | 사용자 지적으로 쿼터뷰 방향 재확인. 180° 정면 배치를 215°로 복구하고 게임 카메라 pitch 55° 유지. 6×6m 방 프레이밍 오소 4.4/점유 반경 0.28·0.45 적용. 검토 이미지도 같은 상대 각도로 교정. 반복 나뭇결·금속 얼룩·각진 침구는 미승인 질감 피드백으로 기록. |
| 2026-09-11 | 사용자가 주인공집 원화를 하이드아웃 기준으로 지정하고 하이드아웃부터 진행하기로 결정. Hideout02 6×6m 첫 시안, 목제 가구/철판/마루/시설 소품, UV·PBR 맵을 적용하고 기존 9시설을 연결. 검토 렌더와 임포트/배치 검사 기록. 낡은 14×9 그레이박스 현황과 미구현 도킹 서술을 현재 상태로 갱신. |
| 2026-09-08 | **기능별 파일 분리 + 화면 수치를 Control Panel로.** 은신처 코드가 파일 하나에 400~500줄씩 뭉쳐 있어 "카메라 크기 좀 줄여줘" 같은 수정에도 고칠 자리를 찾기 어려웠다. `partial`로 기능 단위 분할: 디오라마 → Core/Camera/Bar/Pose/Panels, 도크 → Core/Layout/Adopt/Facility/Build, 재배치 → Core/Rules/Ops/Session. 동작은 그대로다(분할만). 카메라 오소·도크 폭/높이/여백·축소 하한은 `GameTuning`("은신처 화면" 그룹)으로 옮겨 **Control Panel에서 재컴파일 없이** 만진다. 문서에 "어디를 고치면 되는가" 파일 지도 추가. |
| 2026-09-08 | **패널 자체 제목 제거 + 내용 확장.** 도크가 좌상단에 시설 이름을 띄우는데 패널도 제 제목을 달고 있어 중복이었다. 제작창의 "작업대"+설명 두 줄(y −8~−56)과 라디오의 Header(64)+구분선을 감추고, **그 자리만큼 아래 내용을 끌어올린다**(제작 탭 −58→−8, 목록·본문 50px 확장). 감추기만 하면 위에 빈 띠만 남는다. 세로로 늘어나는 요소는 옮기는 대신 위로 자라게 한다. 이름 검색은 **바로 아래 자식에만** 건다 — 자손 전체를 뒤지면 목록 항목 안의 "Title"까지 사라진다. 침대("수면")·창고는 제목이 무게 표시 등과 붙어 있어 그대로 둔다. |
| 2026-09-08 | **창고는 창고 격자만.** 창고 패널은 원본이 캐릭터 상태(x345) + 장비·소지품(x959) + 창고 격자(x1579) 3열이라, 세로로 쌓으면 캐릭터 상태가 맨 위에 오고 정작 창고가 스크롤 아래로 밀렸다. 캐릭터 상태·장비·소지품을 감추고 **창고 격자만** 남긴다 — 인벤토리·장비는 Tab에서 본다. 세로 초과가 15% 이내면 스크롤 대신 살짝 줄여 한 화면에 담는다(창고 928 vs 도크 856 = 8%). 도크 크기는 다시 **전 시설 840×960 동일**. |
| 2026-09-08 | **침대도 우측 도크로 통일.** 침대만 하단 바를 쓰다 보니 수면창(720×460)이 커서 바를 높여야 했고, 그만큼 방이 다른 시설보다 작게 잡혀 화면 크기가 시설마다 들쭉날쭉했다. 전 시설 우측 도크로 맞췄다. 침대 전용 예외(하단 바 높이, 스크롤 대신 눌러 담기)는 제거. 프레이밍 기준값에 여유를 줬다(0.29/0.33 → 0.31/0.35) — 딱 맞추면 방 벽이 화면 끝에 붙어 잘린다. |
| 2026-09-08 | **도크 레이아웃 재구성.** 도크를 560→**840폭(창고 880)·960높이**로 키우고, 입양한 패널이 도크 배경을 **꽉 채우도록** 바꿨다. 제목은 좌상단, **닫기는 우상단 X**(아래 버튼 제거 — 세로 100px 회수). 상단에 **시설 바로가기 바**를 추가(소품 클릭과 동일 동작, 현재 시설 강조). 시설 UI가 열려 있으면 **나가기를 막는다**(제작·파견 중 씬 전환 방지). 카메라 프레이밍은 `HideoutDockPanel.FreeScreenRect`(도크·상단 띠를 뺀 빈 영역)에 방을 맞추는 방식으로 일반화 — 도크 크기를 바꿔도 방이 UI 밑으로 들어가거나 잘리지 않는다. |
| 2026-09-08 | **전체화면 UI → 도크 폭 재배치**(`DockedLayout`). 실측: 제작 `CraftRoot` 1920은 껍데기고 내용은 `CenterPanel` 520×480 / 침대 `Window` 720×460 / 파견 `LeftPanel` 360+지도 1464 / 라디오 540+1276 2열 / 창고 643+556+556 3열. ① 껍데기를 지나 내용까지 **내려가고**(껍데기의 배경 그림은 꺼서 암막·클릭 가로채기 방지) ② 도크에서 쓸모없는 것(파견 지도, 패널 자체 닫기)을 **덜어내고** ③ 도크보다 넓은 노드는 자식을 **세로로 쌓고**(다단→단일 컬럼) ④ 그래도 세로가 넘치면 **스크롤**. 침대는 버튼이 중앙 앵커라 예외적으로 눌러 담는다. 건드린 값은 전부 `Session`에 기록해 도크를 닫을 때 되돌린다 — 안 되돌리면 은신처 밖에서 그 UI가 찌그러져 뜬다. |
| 2026-09-08 | **레시피·스테이션 배선 + 기능 버튼.** 도크에 시설별 **기능 버튼**을 붙였다(작업대=제작·수리, 요리대=요리, 의료대=조제, 창고=창고 열기, 라디오=청취, 파견=파견, 침대=휴식, 발전기=전력 전환). 제작 3종은 `CraftingStation` enum(Workbench/CookingBench/MedicalBench)으로 갈라져 `UIManager.ShowCrafting`으로 들어간다. 버튼은 **Lv≥1(건설 완료)일 때만** 뜬다. 기능 버튼이 연 UI는 전부 도크 안으로 입양(`Adopt`)돼 캐릭터를 덮지 않는다. 검증: 8개 시설 전부 실제 클릭으로 통과(라디오·파견은 **발전기 전력** 게이트가 정상 작동 — 전력 없으면 토스트로 거부). |
| 2026-09-08 | **배치 재설계 + 역할 라벨.** 소품이 겹치던 2D 좌표 직행을 버리고 벽에 줄 세워 가운데를 비웠다. 소품 머리 위 `FacilityLabel` 상시 표시. **근접 E 프롬프트를 끈다**(`InteractionSystem.enabled=false`) — 디오라마가 `HideoutController`의 경로를 양보받으면서 이 처리가 같이 빠져 있었다. |
| 2026-09-08 | **오케스트레이터 구현**(`HideoutDiorama`). 3D 레이캐스트 클릭 → 앵커로 이동·응시 → `CameraFollow.SetFocus`로 UI 자리 확보 → 기존 `InteractableObject.Interact` 호출. 빌더가 시설 9곳에 `InteractableObject`(Bed/Workbench/Stash/Radio/CookingBench/MedicalBench/Dispatch/Generator)를 배선. 방이 14×9m뿐이라 줌인 시 카메라가 방 밖(검은 공백)을 잡아 **70×70 배경판** 추가, 포커스 오소 4.5→6.0. |
| 2026-09-08 | **상호작용 = 디오라마 확정.** 걸어다니지 않고 클릭 유지하되 캐릭터를 숨기지 않는다 — 시설마다 캐릭터가 가서 자세를 잡는다(침대=눕기, 요리대=앞에 서기, 대기=의자에 앉기). **UI는 캐릭터를 가리지 않는다**(우측 도킹/하단 바, 카메라가 부드럽게 밀어 자리 비움). `HideoutFacilityAnchor` + `CameraFollow.SetFocus` 구현, 빌더에 앵커 9개 배선. |
| 2026-09-08 | 3D 은신처 그레이박스 최초 제작(14×9m, 벽 2.6m, 천장 없음). 시설 8종 좌표를 2D판과 1:1로 배치. 골판 리브·전구/천창 조명. 샌드박스 테스트 씬 폐기. |
