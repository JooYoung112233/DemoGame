# 은신처 3D — 공간 규격 · 소품 정의서

> 3D 쿼터뷰 전환의 **첫 실제 공간**. 2026-09-08 그레이박스 제작.
> 빌더: `Assets/Editor/Hideout3DLayout.cs` (메뉴 `Tools ▸ TopDown ▸ 빌드 ▸ 은신처 (3D)`)
> 씬: `Assets/Scenes/Hideout3D.unity` — **빌더 산출물이다. 손으로 고치지 말고 빌더를 고칠 것.**
>
> 좌표 규약: 월드 = **XZ 평면**, 위 = **+Y**, **1u = 1m**. 2D판 좌표 `(x, y)`를 `(x, 0, y)`로 그대로 옮겨
> 시설 배치가 [2D판](safehouse.md)과 1:1로 대응한다. 관련: [3d-migration.md](3d-migration.md)

## 결정

| 날짜 | 질문 | 결정 |
|---|---|---|
| 2026-09-08 | 3D 전환의 첫 공간을 무엇으로? | **은신처**. 샌드박스 테스트 씬은 폐기. `safehouse.md`의 "3D 은신처는 캐릭터 확인 이후" 계획대로 |
| 2026-09-08 | 실내에 천장을 만들 것인가? | **만들지 않는다.** pitch 55° 쿼터뷰에서 지붕이 있으면 내부가 아예 안 보인다. "안에 있다"는 느낌은 **4면 벽 + 어두운 조명 + 화면 가장자리 암전**이 낸다 |

## 상호작용 — 디오라마 (2026-09-08 확정)

은신처는 **걸어다니지 않는다.** 2D판의 타르코프식 클릭 방식을 유지한다.
다만 2D판처럼 캐릭터를 **숨기지 않는다** — 클릭한 시설로 캐릭터가 가서 자세를 잡는 **디오라마**다.

| 시설 | 캐릭터 자세 | UI 자리 |
|---|---|---|
| (대기) 의자 | **앉아 있음** | — |
| 침대 | **눕는다** | **하단** — 화면을 가리지 않는 바 또는 시간 표기 |
| 요리대 | 앞에 선다 | **우측 패널** |
| 작업대·의료대·창고·라디오·파견·발전기 | 앞에 선다 | **우측 패널** |

### 원칙: UI가 캐릭터를 가리지 않는다
캐릭터가 시설에서 자세를 잡는 것이 이 화면의 요점이므로, 그 위에 패널을 덮으면 의미가 없다.

- UI는 **우측 도크**(840×960, 전 시설 동일)로 붙인다. 전체화면 패널 금지.
  시설마다 다른 자리에 띄우면 화면 크기가 들쭉날쭉해 산만하다 — **전 시설 동일**이다.
  (`Dock.Bottom`은 코드에 남아 있지만 쓰는 시설이 없다. 짧은 상태 표시가 필요해지면 그때 쓴다.)
- 도크 안: **좌상단 시설 이름 · 우상단 닫기 X · 나머지는 내용이 꽉 채운다.**
- 시설을 누르면 **카메라가 부드럽게 이동해** 방을 도크 반대쪽으로 밀고, 그 영역에 맞게 줌아웃한다.
  순간이동이 아니라 스무딩 이동이다.
- 화면 상단에 **시설 바로가기 바**가 있다. 소품 클릭과 완전히 같은 동작이다.

### 구현

| 구성 | 파일 | 역할 |
|---|---|---|
| **오케스트레이터** | `Scripts/TopDown/HideoutDiorama.cs` | 시설 클릭(3D 레이캐스트) → 캐릭터 이동·시설 응시·자세 → 카메라 포커스 → 기존 UI 열기. 씬에 있으면 `HideoutController`가 **캐릭터 숨김·카메라 고정·2D 클릭 판정을 양보**한다(`HideoutDiorama.Active`) |
| **역할 라벨** | `Scripts/TopDown/FacilityLabel.cs` | 소품 머리 위에 "침대·작업대·창고…"를 **상시 표시**. 걸어다니지 않는 화면이라 근접 E 프롬프트가 성립하지 않는다 — 무엇을 누를 수 있는지 한눈에 보여야 한다. `TextMesh`+`LegacyRuntime.ttf`, 카메라 향해 빌보드 |
| 시설 앵커 | `Scripts/TopDown/HideoutFacilityAnchor.cs` | 시설별 **캐릭터가 설 자리·자세(Stand/Sit/Lie)·UI 도킹(Right/Bottom/None)**. `moduleKey`는 `HideoutUI.Show(module)` 값과 일치해야 한다 |
| 카메라 연출 | `CameraFollow.SetFocus(focus, screenBias, orthoSize)` | 피사체를 화면 비율만큼 밀어 UI 자리를 비운다. `ClearFocus()`로 복귀. 도킹에서 편향 자동 산출(우측→캐릭터 왼쪽, 하단→캐릭터 위) |

### ⚠️ 아직 안 된 것

- **앉기·눕기 애니메이션이 없다.** 위치·응시 방향은 맞지만 자세는 idle이다.
  `HideoutDiorama.ApplyPose`는 애니메이터에 같은 이름(Sit/Lie)의 트리거가 **있을 때만** 건다 —
  없으면 조용히 넘어가므로, 클립이 제작되면 트리거만 추가하면 자동으로 살아난다.
- **기존 UI 패널이 전체화면 전제**다. 도킹(우측/하단)은 앵커에 정의됐고 카메라도 자리를 비우지만,
  패널 자체를 그 자리에 맞게 재배치하는 작업이 남았다.
- **자세 애니메이션이 없다.** 치비는 idle/walk/run 3종뿐 — 앉기·눕기·작업 자세 미제작
  ([3d-migration.md](3d-migration.md) Stage 4). 그전까지는 위치·방향만 맞고 자세는 idle이다.
- **기존 UI 패널들이 전체화면 전제**다. 요리·수면·창고 등을 우측/하단 도킹으로 재배치해야 한다.

## 공간 규격

| 항목 | 값 | 비고 |
|---|---|---|
| 방 크기 | **14 × 9 m** | 2D판 `MapW 14 / MapH 9`와 동일 |
| 벽 높이 | **2.6 m** | 실제 해상 컨테이너 내부고(약 2.4~2.7m) |
| 벽 두께 | 0.3 m | |
| 천장 | **없음** | 위 결정 참조 |
| 진입 스폰 | `(3, 0, 4.5)` | 2D판 `default` 스폰과 동일 |
| 캐릭터 키 | 2.08 m (스케일 1.0) | 방 높이 2.6m 대비 — 사람이 꽉 차 보인다(의도) |

## 소품 목록 — 모델 제작용

치수는 **가로(X) × 높이(Y) × 세로(Z)**, 미터. 위치는 바닥 기준 `(x, z)` — 빌더가 높이 절반만큼 띄워 바닥에 앉힌다.
현재는 전부 **단색 박스 그레이박스**다. 아래 "형태 메모"가 실제 모델 제작 지시다.

### 구조물

| 이름 | 치수 | 위치 | 형태 메모 |
|---|---|---|---|
| `Floor` | 14 × 0.1 × 9 | 중앙 | 골판 금속 바닥. 긁힘·녹·기름때. 가운데가 닳아 색이 옅어진 동선 |
| `Wall_S` / `Wall_N` | 14.6 × 2.6 × 0.3 | 남/북 | 컨테이너 측벽 |
| `Wall_W` / `Wall_E` | 0.3 × 2.6 × 9 | 서/동 | 동벽 = 컨테이너 **양문**(경첩·잠금바). 서벽 = 막힌 면 |
| `Rib_S` / `Rib_N` | 0.18 × 2.4 × 0.10 | 1.2 m 간격 | **골판 리브**. 콜라이더 없음(장식). 실제 컨테이너의 세로 주름 |

> 리브는 지금 얕은 박스를 덧댄 것이다. 실제 모델에서는 **벽 메시 자체에 주름을 넣어** 드로우콜을 줄일 것.

### 배치 원칙 (2026-09-08 재설계)

소품을 **북벽·남벽·동벽에 붙여 줄을 세우고 가운데를 비운다.** 2D 좌표를 그대로 옮겼더니
소품이 서로 겹치고 대기 의자가 발전기에 붙어 있었다. 가운데 빈 공간이 대기 자리 겸
카메라가 방을 잡을 때의 여백이 된다.

| 벽 | 소품 |
|---|---|
| 북 (z≈7.8) | 침대 · 라디오 · 작업대 · 창고 |
| 남 (z≈1.3) | 의료대 · 조리대 · 파견 보드 |
| 동 (x≈12.9) | 발전기 |
| 중앙 (6.6, 4.5) | 대기 의자 |

캐릭터가 서는 자리는 소품에서 **방 중앙 쪽으로 1.15 m**. 대기 의자만 예외로 오프셋 0(앉는 자리).

### 시설 8종 — 전부 상호작용 대상

시설은 게임 시스템에 물려 있다. 이름을 바꾸면 `HideoutController`/`HideoutModuleManager` 연결이 끊긴다.

| 이름 | 치수 | 위치 | 기능 | 형태 메모 |
|---|---|---|---|---|
| `Bed_BoxCot` | 2.2 × 0.55 × 1.1 | (2.6, 7.7) | 수면·휴식 | 나무 팔레트 위 야전 매트. 담요 구겨짐, 머리맡에 깡통 |
| `Workbench_Broken` | 1.9 × 0.95 × 0.8 | (8.4, 7.8) | 제작·수리 | **부서진** 작업대 — 상판 한쪽이 내려앉고 다리를 각목으로 덧댐. 만력바이스, 공구 몇 개 |
| `Stash_창고` | 1.3 × 1.4 × 0.8 | (11.6, 7.8) | 보관함(메인 스태시) | 금속 캐비닛 또는 쌓아올린 궤짝. 자물쇠 |
| `Radio_라디오` | 0.8 × 0.6 × 0.6 | (5.6, 7.9) | 정보 수신 | 군용 무전기. 안테나, 다이얼, 계기 바늘. **전력 필요** |
| `CookingBench` | 1.7 × 0.9 × 0.8 | (6.6, 1.3) | 조리 | 드럼통 화덕 + 그을린 냄비. 연통이 벽으로 |
| `MedicalBench` | 1.7 × 0.9 × 0.8 | (3.0, 1.3) | 의료 | 흰 법랑 상판, 거즈·주사기·소독약. 가장 깨끗한 물건 |
| `Dispatch_파견` | 1.2 × 1.6 × 0.3 | (10.2, 1.15) | NPC 출전 | 벽걸이 **게시판**. 압정 꽂은 쪽지, 지도 조각, 실 연결 |
| `Generator_발전기` | 1.0 × 1.0 × 1.0 | (12.9, 4.5) | 전력 ON/OFF | 소형 발전기. 연료통, 배기구, 당김줄. 라디오·파견의 전제 |

### 조명

| 이름 | 종류 | 위치 | 값 | 메모 |
|---|---|---|---|---|
| `KeyLight` | Directional | — | 강도 0.55, 색 (0.80, 0.86, 1.00), 각도 (62, −30, 0) | 위에서 새어드는 **찬** 빛. 천장 틈새 설정 |
| `Bulb` | Point | (7, 2.3, 4.5) | 강도 3.2, 반경 11, 색 (1.00, 0.82, 0.58) | 매달린 **백열 전구**. 실제 모델에선 전선·소켓·갓 필요 |

앰비언트는 Trilight — sky (0.20, 0.22, 0.26) / equator (0.15, 0.15, 0.17) / ground (0.09, 0.09, 0.10).
찬 자연광 + 따뜻한 전구의 **색 대비**가 이 방의 인상을 만든다. 모델·재질 작업 시 유지할 것.

## 아직 안 된 것

- **시설 클릭 판정이 아직 없다.** 앵커(`HideoutFacilityAnchor`)는 붙었지만 마우스 클릭 →
  캐릭터 이동 → UI 열기로 잇는 오케스트레이터가 없다(위 §디오라마 참조).
- **컨테이너 문 없음.** 동벽이 막혀 있다. 진출입 연출(문 열림/씬 전환)은 후속.
- **재질이 단색**이다. 텍스처·노멀은 3D 아트 패스에서.
- 캐릭터 가독성 — 어두운 팔레트가 배경에 묻힌다. 아웃라인/접지 링 검토(3d-migration.md Stage 0 함정 ③).

## 미결

1. 컨테이너 문 위치·진출입 연출. 마을 쪽 외관은 `Container_Home`(64, 12) 서쪽에 문이 있다.
2. 은신처를 **진짜 컨테이너 비율**(12 × 2.4 m)로 좁힐지, 지금의 방 크기(14 × 9)를 유지하고
   이름을 바꿀지 → [building-interior.md](building-interior.md) §스케일. 현재는 마을의 컨테이너
   외관 발자국을 14 × 9로 맞춰 **모순만 해소**한 상태다.

## 변경 로그

| 날짜 | 내용 |
|---|---|
| 2026-09-08 | **패널 자체 제목 제거 + 내용 확장.** 도크가 좌상단에 시설 이름을 띄우는데 패널도 제 제목을 달고 있어 중복이었다. 제작창의 "작업대"+설명 두 줄(y −8~−56)과 라디오의 Header(64)+구분선을 감추고, **그 자리만큼 아래 내용을 끌어올린다**(제작 탭 −58→−8, 목록·본문 50px 확장). 감추기만 하면 위에 빈 띠만 남는다. 세로로 늘어나는 요소는 옮기는 대신 위로 자라게 한다. 이름 검색은 **바로 아래 자식에만** 건다 — 자손 전체를 뒤지면 목록 항목 안의 "Title"까지 사라진다. 침대("수면")·창고는 제목이 무게 표시 등과 붙어 있어 그대로 둔다. |
| 2026-09-08 | **창고는 창고 격자만.** 창고 패널은 원본이 캐릭터 상태(x345) + 장비·소지품(x959) + 창고 격자(x1579) 3열이라, 세로로 쌓으면 캐릭터 상태가 맨 위에 오고 정작 창고가 스크롤 아래로 밀렸다. 캐릭터 상태·장비·소지품을 감추고 **창고 격자만** 남긴다 — 인벤토리·장비는 Tab에서 본다. 세로 초과가 15% 이내면 스크롤 대신 살짝 줄여 한 화면에 담는다(창고 928 vs 도크 856 = 8%). 도크 크기는 다시 **전 시설 840×960 동일**. |
| 2026-09-08 | **침대도 우측 도크로 통일.** 침대만 하단 바를 쓰다 보니 수면창(720×460)이 커서 바를 높여야 했고, 그만큼 방이 다른 시설보다 작게 잡혀 화면 크기가 시설마다 들쭉날쭉했다. 전 시설 우측 도크로 맞췄다. 침대 전용 예외(하단 바 높이, 스크롤 대신 눌러 담기)는 제거. 프레이밍 기준값에 여유를 줬다(0.29/0.33 → 0.31/0.35) — 딱 맞추면 방 벽이 화면 끝에 붙어 잘린다. |
| 2026-09-08 | **도크 레이아웃 재구성.** 도크를 560→**840폭(창고 880)·960높이**로 키우고, 입양한 패널이 도크 배경을 **꽉 채우도록** 바꿨다. 제목은 좌상단, **닫기는 우상단 X**(아래 버튼 제거 — 세로 100px 회수). 상단에 **시설 바로가기 바**를 추가(소품 클릭과 동일 동작, 현재 시설 강조). 시설 UI가 열려 있으면 **나가기를 막는다**(제작·파견 중 씬 전환 방지). 카메라 프레이밍은 `HideoutDockPanel.FreeScreenRect`(도크·상단 띠를 뺀 빈 영역)에 방을 맞추는 방식으로 일반화 — 도크 크기를 바꿔도 방이 UI 밑으로 들어가거나 잘리지 않는다. |
| 2026-09-08 | **전체화면 UI → 도크 폭 재배치**(`DockedLayout`). 실측: 제작 `CraftRoot` 1920은 껍데기고 내용은 `CenterPanel` 520×480 / 침대 `Window` 720×460 / 파견 `LeftPanel` 360+지도 1464 / 라디오 540+1276 2열 / 창고 643+556+556 3열. ① 껍데기를 지나 내용까지 **내려가고**(껍데기의 배경 그림은 꺼서 암막·클릭 가로채기 방지) ② 도크에서 쓸모없는 것(파견 지도, 패널 자체 닫기)을 **덜어내고** ③ 도크보다 넓은 노드는 자식을 **세로로 쌓고**(다단→단일 컬럼) ④ 그래도 세로가 넘치면 **스크롤**. 침대는 버튼이 중앙 앵커라 예외적으로 눌러 담는다. 건드린 값은 전부 `Session`에 기록해 도크를 닫을 때 되돌린다 — 안 되돌리면 은신처 밖에서 그 UI가 찌그러져 뜬다. |
| 2026-09-08 | **레시피·스테이션 배선 + 기능 버튼.** 도크에 시설별 **기능 버튼**을 붙였다(작업대=제작·수리, 요리대=요리, 의료대=조제, 창고=창고 열기, 라디오=청취, 파견=파견, 침대=휴식, 발전기=전력 전환). 제작 3종은 `CraftingStation` enum(Workbench/CookingBench/MedicalBench)으로 갈라져 `UIManager.ShowCrafting`으로 들어간다. 버튼은 **Lv≥1(건설 완료)일 때만** 뜬다. 기능 버튼이 연 UI는 전부 도크 안으로 입양(`Adopt`)돼 캐릭터를 덮지 않는다. 검증: 8개 시설 전부 실제 클릭으로 통과(라디오·파견은 **발전기 전력** 게이트가 정상 작동 — 전력 없으면 토스트로 거부). |
| 2026-09-08 | **배치 재설계 + 역할 라벨.** 소품이 겹치던 2D 좌표 직행을 버리고 벽에 줄 세워 가운데를 비웠다. 소품 머리 위 `FacilityLabel` 상시 표시. **근접 E 프롬프트를 끈다**(`InteractionSystem.enabled=false`) — 디오라마가 `HideoutController`의 경로를 양보받으면서 이 처리가 같이 빠져 있었다. |
| 2026-09-08 | **오케스트레이터 구현**(`HideoutDiorama`). 3D 레이캐스트 클릭 → 앵커로 이동·응시 → `CameraFollow.SetFocus`로 UI 자리 확보 → 기존 `InteractableObject.Interact` 호출. 빌더가 시설 9곳에 `InteractableObject`(Bed/Workbench/Stash/Radio/CookingBench/MedicalBench/Dispatch/Generator)를 배선. 방이 14×9m뿐이라 줌인 시 카메라가 방 밖(검은 공백)을 잡아 **70×70 배경판** 추가, 포커스 오소 4.5→6.0. |
| 2026-09-08 | **상호작용 = 디오라마 확정.** 걸어다니지 않고 클릭 유지하되 캐릭터를 숨기지 않는다 — 시설마다 캐릭터가 가서 자세를 잡는다(침대=눕기, 요리대=앞에 서기, 대기=의자에 앉기). **UI는 캐릭터를 가리지 않는다**(우측 도킹/하단 바, 카메라가 부드럽게 밀어 자리 비움). `HideoutFacilityAnchor` + `CameraFollow.SetFocus` 구현, 빌더에 앵커 9개 배선. |
| 2026-09-08 | 3D 은신처 그레이박스 최초 제작(14×9m, 벽 2.6m, 천장 없음). 시설 8종 좌표를 2D판과 1:1로 배치. 골판 리브·전구/천창 조명. 샌드박스 테스트 씬 폐기. |
